---
title: Meridian compatibility contract
document_type: contract
status: maintained
scope: workspace
owner: workspace-owner
created: 2026-08-18
updated: 2026-09-25
---

# Meridian compatibility contract

Совместимость **объявляется**, а не выводится из близости номеров версий.

При миграции Node.js → Rust совместимость означает сохранение объявленной
бизнес-ценности, форматов обмена и публичных гарантий. Она не требует
воспроизводить внутреннюю структуру, динамические слабости или известные
дефекты Node.js. Намеренное Rust-native усиление допустимо и обязательно,
когда делает реализацию лучше или надёжнее; такое отличие документируется,
обосновывается и покрывается тестом по
`standards/workspace/rust-migration-quality.md`.

## Релизные единицы

Meridian состоит из независимо версионируемых единиц. Общей линии версий у них нет.

| Единица | Где | Линия версий | Примечание |
|---|---|---|---|
| Kernel | этот репозиторий | SemVer, `VERSION` | методология, валидатор, схемы, writers |
| Instance | отдельный репозиторий на продукт, вне Kernel | Git revision; SemVer только если распространяется | размещается там, где данные продукта; никогда не поставляется вместе с Kernel |
| Завендоренные skills | `skills/<name>/` | неизменяемый SHA-256 в `PIN.yaml` | пин, а не номер версии |
| Smoke-пакет | Instance | собственный SemVer | product-specific по природе |

## Kernel ↔ Instance

| Kernel | Ожидаемый Instance | Ломающие изменения |
|---|---|---|
| `0.1.x` | `schema_version: 1` в `product.yaml` | не объявлены; `0.x` не даёт гарантий стабильности |
| `0.2.x` | `schema_version: 1` в `product.yaml`; action-профили `environments/access.yaml` используют generic-enum или `x-*`-префикс для продуктовых действий | относительно `0.1.x`: enum действий сужен, продуктовые значения без префикса `x-*` не проходят schema-gate — Instance требует миграции (см. `CHANGELOG.md` 0.2.0 → Breaking) |
| `0.4.x` | то же, что `0.2.x`; плюс: рабочие заметки Instance, ссылающиеся на слой шаблонов, указывают на `standards/templates/template-contract.md` и `standards/templates/profiles/<платформа>/<тип>-body.md`; плюс: каждая запись `inventory/repositories.yaml` несёт `profile`, значение которого названо из закрытого пула Kernel stack profiles и подтверждается манифестом соответствующего репозитория | относительно `0.3.x`: слой шаблонов разделён на контракт и профиль, после чего всё дерево приведено к единому правилу имён (`standards/workspace/document-identity.md`). Переименовано шестнадцать файлов, в том числе `CONFLUENCE.md` → `profiles/confluence/confluence-profile.md`, восемь `*.confluence-template.md` → `profiles/confluence/*-body.md`, `PROTOCOL.md` → `smoke-protocol.md`, `ACCEPTANCE-GATE.template.md` → `acceptance-gate-template.md`, `BOOTSTRAP.md` → `instance-bootstrap.md`. Ломает текстовые ссылки Instance, и гейт этого не видит — см. следующий раздел. Плюс относительно `0.3.x`: `registries/inventory/repositories.schema.json` сделал `repositories[].profile` обязательным; отсутствие `profile`, имя вне закрытого пула Kernel stack profiles или объявление, противоречащее манифесту репозитория, даёт FAIL валидатора и требует миграции Instance |
| `0.5.x` | сохраняет все требования `0.4.x`; плюс, при переходе на Kernel `0.5.x`: временные ветви Instance соответствуют закрытому шаблону имён временных ветвей (`standards/workspace/version-control-flow.md` §3, §3.1); сообщения новых коммитов Instance используют форму сообщений коммитов Meridian — типизированный заголовок и четыре раздела тела (§12); принятие этих общих норм конкретным Instance оформляется отдельным репозиторий-локальным пакетом Instance. Instance **не обязан** переименовывать свои стабильную и интеграционную линии в `main` / `dev` и **сохраняет имена своих линий из собственного tracked-источника** (§1.2): постоянные линии `main` / `dev`, их защита и операционное переименование `master` → `main` / `develop` → `dev` относятся только к репозиторию Kernel | относительно `0.4.x` (пакет `git-governance-migration`, см. `CHANGELOG.md` 0.5.0 → Breaking): временная ветвь вне разрешённого закрытого набора норме больше не соответствует; неанглийское имя, транслитерация, верхний регистр и неправильный kebab-case норме не соответствуют; новые коммиты обязаны использовать установленный заголовок и четыре раздела тела; переход конкретного Instance требует отдельного репозиторий-локального пакета, но **не** переписывания и не переименования его существующей истории и ветвей |
| `0.6.x` | сохраняет все требования `0.5.x`; описывает три аспекта совместимости операционной модели `0.6.x`. Эти требования применяются **при фактическом использовании** соответствующей новой модели, а не ретроактивно ко всякому старому Instance просто потому, что Kernel продвинулся до `0.6.x`. **Контракты Ядра**: модель областей рабочих данных (`standards/workspace/workspace-scope-model.md`) и объединённая квалификация совместимости рабочих пространств (`standards/workspace/workspace-compatibility-qualification.md`, закрытый вердикт `QUALIFIED`/`BLOCKED`/`UNVERIFIED`), которая композирует пять уже обязательных для Ядра участков — реестр источников инструкций (`instruction-source-registry`), приём внешних правил (`controlled-rule-intake`), режим подключения существующего проекта (`existing-project-compatibility-mode`) и контракт миграции данных Экземпляра с его каноническим экспортом (`instance-data-migration`, `instance-canonical-export-contract`); плюс отдельная линия операционной модели, также обязательная часть Ядра: каталог типов задач (`task-pattern-registry.md`), контракт постановки задачи (`task-specification.md`), многоосевая модель состояния выполнения (`execution-state-model.md`), роли и управление человеком (`role-and-human-control.md`), ограниченный манифест контекста (`bounded-context-manifest.md`), доказательства и передача результата (`evidence-and-handoff-contract.md`), полевая оценка без единого итогового балла — восемь раздельных характеристик без свёртки в одну цифру (`meridian-field-evaluation.md`) и объединённая квалификация обновления интеграции с закреплёнными (`sha256`-pinned) ссылками на составленные записи, а не встроенными копиями (`upgrade-integration-qualification.md`). Её поле `workspace_transition_compatibility` — обязательный, но продуктово-нейтральный шаблон: Kernel предоставляет только его, закрытым в этом срезе к единственному значению `kernel-template-only`. **Доказательства Экземпляра**: заполненное доказательство перехода конкретного продукта на новую модель рабочих пространств принадлежит отдельному, более позднему пакету Экземпляра — не этому Kernel-срезу. При подключении через модель рабочих пространств Instance предъявляет проверяемые записи, а не заявления — зарегистрированный источник инструкций, кандидата правила с явным решением владельца (обнаружение или чтение источника или кандидата правила само по себе полномочия нормы не даёт), план обнаружения существующего проекта, который не вносит и не изменяет ни одной записи в файлах подключаемого проекта автоматически, и, если заявлена миграция, план миграции с каноническим экспортом; композитная квалификация остаётся `UNVERIFIED`, пока хотя бы один разрешённый кандидат правила не получил решения владельца. **Переходная совместимость**: `standards/workspace/kernel-boundary.md` по-прежнему признаёт отдельный Экземпляр и `MERIDIAN_INSTANCE` действующим и **поддерживаемым** переходным адаптером и больше не объявляет разделение на два Git-репозитория целевой архитектурой или требованием для нового проекта — Instance, уже использующий такое разделение, продолжает соответствовать норме без обязательной консолидации. Ни объединённая квалификация, ни контракт миграции данных не переносят и не запускают перенос продуктовых данных сами и не выбирают постоянное хранилище этим выпуском — они определяют и проверяют независимый от хранилища контракт; положительный вердикт требует предъявленных и разрешённых записей, а не отсутствия найденных проблем. Пакет `meridian-operating-foundation` и последующие пакеты этой линии не начинают, не расширяют и не изменяют PHASE G или Concord. Принятие этой строки конкретным выпускаемым Instance подтверждается не фактом выпуска Kernel `0.6.x`, а отдельной repository-local `revision-promotion`-процедурой самого этого Instance (`version-control-flow.md` §1.3, §2.3) | относительно `0.5.x`: явных Breaking-изменений не заявлено. Исполнитель и Git-интегратор перестают быть привязаны к конкретной модели или приложению (`version-control-flow.md` §5) — их наличие и распределение для конкретного Instance устанавливает принятый им repository- или task-local протокол, а не универсальная норма Ядра. Механизм проверки имени ветви (§13.2) сужен до проверки исходной ветви merge request на уже обязательном участке — закрытый шаблон имён (§3, §3.1) и топология ветвей не изменены |

Пока Kernel в `0.x`, любой minor может сломать Instance. Instance пиннит точную
версию Kernel до выхода `1.0.0`.

## Instance продукта и прежний Instance разработки Meridian

Таблица выше — о произвольном продуктовом Instance любого проекта,
использующего Meridian: он остаётся поддерживаемым переходным адаптером под
`0.6.x`, как и было. Ничего в этом контракте не объявляет продуктовые Instance
немедленно запрещёнными или устаревшими.

Отдельно от этого: конкретный прежний Instance, ранее служивший центром
управления разработкой самого Meridian (а не продукта), с 2026-09-19 заморожен
для чтения (`AGENTS.md` §10; `governance/meridian-owner-intent-contract.md`
§25) — он больше не активный источник планов, назначений или Git-процесса этой
разработки. Это решение о разработке Meridian, а не о совместимости Kernel с
продуктовыми Instance в целом.

Окончательный рубеж, после которого чтение `MERIDIAN_INSTANCE` для работы над
Meridian перестаёт быть нужным вовсе (включая переходный
`scripts/preflight.mjs --require-instance`), — проверенный импорт пакета 8
(`meridian-cli-migration`) программы `meridian-rust-migration` и отключение
эксплуатационных чтений через `MERIDIAN_INSTANCE`
(`governance/plans/meridian-rust-migration-program-plan.md` §6.5a, §7). До
этого рубежа замороженный источник остаётся читаемым только явно и только для
миграционных целей.

## Ссылки Instance на пути внутри Kernel

Гейт Kernel проверяет две вещи и не проверяет третью:

- относительные Markdown-ссылки **внутри** Kernel — проверяются: несуществующая
  цель красит прогон;
- относительная ссылка из Kernel, уходящая **за** его границу, — дефект по
  правилу, даже если на текущей машине она открывается;
- **текстовая ссылка из Instance на путь внутри Kernel** (`$MERIDIAN_KERNEL/…`)
  — не проверяется ничем. Это не Markdown-ссылка, и разрешать её валидатору не
  с чем: Instance для него — данные, а не исходник, и целевого корня у такой
  ссылки в общем случае нет.

Следствие, а не недосмотр: переименование файла в Kernel может молча сломать
рабочую заметку Instance, а прогон останется зелёным. Поэтому перемещения в
нормативных каталогах Kernel объявляются здесь строкой в таблице выше, а не
выводятся из того, что гейт не покраснел. Владелец Instance правит свои ссылки
по объявленной строке.

Исторические записи Instance (отчёты, архив анализа) при таком перемещении **не
переписываются**: они фиксируют путь, который существовал на момент записи. По
той же причине, по которой не переписывается `CHANGELOG.md`.

## Намеренные Rust-native усиления над Node.js-эталоном

Записи реестра намеренных наблюдаемых расхождений Rust-реализации с
Node.js-эталоном, каждое — принятое Rust-native усиление
(`standards/workspace/rust-migration-quality.md`), не дефект переноса.
Каждая запись обоснована, ограничена и покрыта положительным и
отрицательным тестом.

| Норма/пакет | Node.js-поведение | Rust-native усиление | Тест |
|---|---|---|---|
| `controlled-rule-intake` (`rust-architecture-conformance-1`) | `scripts/lib/controlled-rule-intake.mjs`'s `checkCluster` сравнивает членов одного `semantic_key`-кластера по `scopeKey = \`${type}::${id}\`` — расхождение по `workspace_id`/`organization_profile_id` молча не замечается | `meridian_core::controlled_rule_intake::checks::check_cluster` сравнивает по полному тождеству `Scope` (те же поля, что уже использует `same_scope`/`checkSupersedes` контракта миграции данных Экземпляра) — два члена кластера, совпадающие по `type`/`id`, но расходящиеся по `workspace_id`, теперь корректно помечаются несогласованными | Rust: `meridian-app/src/operating_model/controlled_rule_intake/mod.rs::tests::a_cluster_whose_members_disagree_only_on_workspace_id_is_flagged`; реальный Node/Rust: `test/conformance-harness.test.mjs`, намеренная граница «cluster scope divergence» — Node принимает мутированный кластер чисто, Rust отклоняет его с диагностикой `more than one scope` |
| `controlled-rule-intake` (`rust-architecture-conformance-1`, корректирующий раунд, item 2) — **библиотечное, не CLI-наблюдаемое** отличие; см. примечание ниже | `evaluateControlledRuleIntake` никогда не останавливается на нарушении схемы (только на исключении компиляции самой схемы) — продолжает вычислять полный составной анализ (id/origin/authority/source_ref/кластеры/граф конфликтов) даже над записью, уже нарушившей свою схему | `evaluate_controlled_rule_intake` останавливается сразу при любом нарушении схемы контейнера ИЛИ записи и не строит доменные объекты вовсе (явное решение владельца, корректирующий раунд `rust-architecture-conformance-1` item 2) — схема-невалидный документ даёт только диагностики схемы | Прямое библиотечное доказательство (третий корректирующий раунд item 6, четвёртый item 3.3), НА ОДНОМ И ТОМ ЖЕ реальном fixture-документе (`registries/operating-model/fixtures/controlled-rule-intake.fixtures.json`'s `valid[0].registry`) с ОДИНАКОВЫМИ двумя мутациями (`registry_id` заменён на неверную непустую строку — отклоняется schema `const`, но принимается closed transport DTO, поскольку `RegistryDto.registry_id` не валидируется; испорчен `origin.source_ref`) на обеих сторонах, не «аналогичным по форме» другим документом (пятый корректирующий раунд, item 1 — прежняя мутация, удаление `classification_basis`, не имела эмпирических зубов на Rust-стороне: это поле одновременно обязательно по схеме и невалидируемо как non-optional `String` в закрытом DTO, так что тест оставался зелёным и при отключённом schema short-circuit): Rust — `meridian-app/src/operating_model/controlled_rule_intake/mod.rs::tests::schema_and_composite_defect_together_produce_exactly_one_schema_diagnostic_matching_the_node_reference` (прямой вызов `evaluate_controlled_rule_intake`, утверждает РОВНО 1 диагностику и явное отсутствие диагностики origin/source_ref); Node — `test/conformance-harness.test.mjs`, блок с прямым импортом `evaluateControlledRuleIntake` из `scripts/lib/controlled-rule-intake.mjs` (минуя `kernel-validate.mjs` целиком), утверждает МИНИМУМ 2 диагностики (схема И origin/source_ref) на том же документе. Отдельно, на исполняемом пути через self-test wrapper: `test/conformance-harness.test.mjs`, «schema short-circuit» — на записи с ОДНОВРЕМЕННО нарушенной схемой И расходящимся `origin.source_ref` полный wrapped-вывод **совпадает** на практике (conformant), потому что и `kernel-validate.mjs` (строка ~1952, `p[0]`), и Rust CLI-обёртка сообщают только ПЕРВУЮ диагностику отклонённой fixture, а диагностика схемы у обеих сторон всегда идёт первой — расхождение в количестве вычисляемых диагностик реально на уровне библиотеки (доказано прямыми тестами выше), но не наблюдаемо на единственном сегодняшнем self-test исполняемом пути (отдельной CLI-команды, читающей произвольный документ controlled-rule-intake напрямую, не существует — реестр остаётся данными Экземпляра) |
| `instruction-source-registry` (`rust-architecture-conformance-2`) — **библиотечное** отличие, наблюдаемое напрямую (нет self-test `p[0]`-обёртки, скрывающей его, как у `controlled-rule-intake`) | `evaluateInstructionSourceRegistry`'s обычный доступ к полям объекта не имеет понятия «закрытая транспортная форма» — лишнее поле в `payload` не мешает библиотеке вычислить остальные бизнес-проверки (например read_channel coherence) над той же записью | Транспортный слой `meridian-app/src/operating_model/instruction_source_registry/dto.rs` — закрытые `#[serde(deny_unknown_fields)]` DTO; при провале `serde`-разбора записи (лишнее поле или несовпадение типа) `convert::build_source` для этой ОДНОЙ записи не вызывается вовсе — сообщается только диагностика разбора, доменные проверки для неё недостижимы, а не просто не вычислены в этом прогоне | Rust: `meridian-app/src/operating_model/instruction_source_registry/mod.rs::tests::a_transport_parse_failure_skips_business_checks_for_that_entry_only` (РОВНО диагностика разбора, явное отсутствие диагностики read_channel); Node: `test/conformance-harness.test.mjs`, прямой импорт `evaluateInstructionSourceRegistry` из `scripts/lib/instruction-source-registry.mjs`, ОДИН И ТОТ ЖЕ реальный fixture-документ (`instruction-source-registry.fixtures.json`'s `valid[1].registry`) с ОДИНАКОВЫМИ двумя мутациями (`payload.unexpected_field` и `payload.read_channel.agent_auto_read`), утверждает ОБЕ диагностики |
| `existing-project-compatibility-mode` (`rust-architecture-conformance-2`) — тот же класс отличия, что и `instruction-source-registry` выше, на уровне записи `workspace_connections[]` | `evaluateExistingProjectCompatibilityMode`'s обычный доступ к полям `payload` не имеет понятия «закрытая транспортная форма» — лишнее поле не мешает библиотеке вычислить остальные проверки (например `next_step`) над той же записью | Транспортный слой `meridian-app/src/operating_model/existing_project_compatibility_mode/dto.rs` — закрытые `#[serde(deny_unknown_fields)]` DTO; при провале `serde`-разбора записи `evaluate_connection` для этой ОДНОЙ записи не вызывается вовсе | Rust: `meridian-app/src/operating_model/existing_project_compatibility_mode/mod.rs::tests::a_transport_parse_failure_skips_business_checks_for_that_entry_only`; Node: `test/conformance-harness.test.mjs`, прямой импорт `evaluateExistingProjectCompatibilityMode`, ОДИН И ТОТ ЖЕ реальный fixture-документ (`existing-project-compatibility-mode.fixtures.json`'s `valid[1].registry`) с ОДИНАКОВЫМИ двумя мутациями (`payload.unexpected_field` и `payload.next_step`), утверждает ОБЕ диагностики |
| `existing-project-compatibility-mode` (`rust-architecture-conformance-2`, корректирующий раунд, item 5) — **принято архитектором** как обоснованное Rust-native сужение, тот же прецедент, что уже принят для `controlled-rule-intake`'s schema short-circuit выше: итоговый вердикт записи не меняется, меняется только состав вторичных диагностик | `sameLocation` (`scripts/lib/existing-project-compatibility-mode.mjs`) сравнивает СЫРЫЕ поля (`path`/`container_ref`/`service_ref`/`resource_ref`) discovery_plan-слота и discovered source БЕЗУСЛОВНО — даже когда сам слот уже признан невалидным собственной `checkPlanLocation` | Rust сравнивает `location` только между ДВУМЯ успешно построенными типизированными `meridian_core::instruction_source::Location` — сравнивать типизированное значение не с чем, если сама запись слота не построилась. Первичный дефект слота (его собственная location-диагностика) и итоговый FAIL записи сохраняются на обеих сторонах без изменений; outcome partition (`check_discovery_outcomes`) по-прежнему учитывает этот слот — он остаётся в `plan_ids` по независимо типизированному `SemanticId`, извлечённому до и без учёта исхода построения `location` (`rust-architecture-conformance-2`, корректирующий раунд, items 3-4). Отсутствует только ВТОРИЧНАЯ диагностика «location does not match» | Rust: `meridian-app/src/operating_model/existing_project_compatibility_mode/mod.rs::tests::a_discovered_source_naming_an_invalid_plan_slot_gets_no_secondary_location_mismatch_diagnostic` (собственный дефект слота присутствует, `no outcome` отсутствует — слот учтён в partition, — а `location does not match` отсутствует); Node: `test/conformance-harness.test.mjs`, прямой импорт `evaluateExistingProjectCompatibilityMode`, ОДИН И ТОТ ЖЕ реальный fixture-документ (`existing-project-compatibility-mode.fixtures.json`'s `valid[1].registry`) с ОДИНАКОВЫМИ двумя мутациями (`discovery_plan[0].path` испорчен на `"../escape.md"`; `discovered_sources[0].payload.location.path` изменён на другой, валидный, но отличающийся путь), утверждает И собственный дефект слота, И диагностику `location does not match` |
| `sha-provenance` (`meridian-cli-foundation-architecture-remediation`, §5.16) — путь скилла ограничен строгим типом, а не строковым соглашением адаптера | `path.join(dir, pin?.artifact ?? '')` и `path.join(dir, arch.path)` (`scripts/kernel-validate.mjs`) не проверяют, что результат остаётся внутри `dir`: пин, называющий `../../<файл>`, переходит границу каталога скилла и верифицируется по файлу СНАРУЖИ него, если такой файл существует и его дайджест совпадает | `meridian_core::types::WorkspaceRelativePath::join` отклоняет `..`, обратный слэш и абсолютный/drive-prefixed путь на построении значения — выходящее имя `artifact`/`source_archive.path` даёт ТОТ ЖЕ текст диагностики, что и обычный отсутствующий файл («which does not exist» / «source archive … is missing», `meridian_core::mechanical_integrity::sha_provenance::{artifact_missing,archive_missing}`), новый текст не появляется; меняется только то, что выходящее имя больше не может быть удовлетворено файлом снаружи каталога скилла | Rust (типовой уровень, без файловой системы): `meridian-core/src/types/workspace_relative_path.rs::tests::rejects_parent_component`; Rust (через порт, позитивный и отрицательный): `meridian-app/src/validation/mechanical_integrity/sha_provenance.rs::tests::{an_escaping_artifact_name_is_reported_as_missing_not_read_from_outside_the_skill, an_escaping_source_archive_path_is_reported_as_missing_not_read_from_outside_the_skill, a_clean_pin_with_a_nested_artifact_path_verifies_positively}`; реальный Node/Rust на ОДНОМ И ТОМ ЖЕ мутированном рабочем дереве (полная копия репозитория, `copyRepoWithoutGitOrTarget`, плюс один добавленный `skills/<name>/PIN.yaml`, для обоих полей `artifact` и `source_archive.path` отдельно): `test/conformance-harness.test.mjs`, блок «sha-provenance path confinement» — Node действительно читает и принимает файл за пределами каталога скилла (пин чист, дайджест совпадает), Rust CLI сообщает `FAIL … which does not exist` / `FAIL … source archive … is missing` для того же пина |
| `sha-provenance` (`meridian-cli-foundation-architecture-remediation`, первый корректирующий раунд, item 2; финализировано четвёртым корректирующим раундом, item 1) — неполный `source_archive` больше не проходит молча | `if (arch?.path && arch?.sha256) { ... }` (`scripts/kernel-validate.mjs`) — когда пин объявляет `source_archive` только с `path` ИЛИ только с `sha256`, весь блок пропускается целиком: ни OK, ни FAIL, ни WARN для source archive этого скилла вообще не выводится | `meridian_core::mechanical_integrity::sha_provenance::build_archive_report` — единственный production construction report builder, вызываемый приложением (`meridian-app/src/validation/mechanical_integrity/sha_provenance.rs`, НЕ вызывающим `validate_pinned_path`/`validate_pinned_digest` независимо): возвращает `ArchiveConstructionReport { path: Result<WorkspaceRelativePath, ArtifactPathError>, sha256: Result<ContentDigest, PinnedDigestError>, complete: Option<SourceArchive> }`, где `complete` строится через реальный `SourceArchive::new` (не тестовый-only API). Приложение считает `path`/`sha256` "missing" (`Err(…Error::Missing)`) — только тогда весь блок "incomplete"; `path`-или-`sha256`-присутствующий-но-невалидный НЕ считается incomplete (тот же truthy-only gate, что и у Node) и всё равно доходит до попытки чтения. Транспортный слой сообщает РОВНО ОДИН FAIL (`source_archive_incomplete`) при missing — половинчатая декларация проверяется против ничего и не верифицирует ничего | Rust: `meridian-core/src/mechanical_integrity/sha_provenance.rs::tests::{source_archive_cannot_be_constructed_with_a_missing_path, source_archive_cannot_be_constructed_with_a_missing_or_invalid_sha256, archive_report_complete_is_none_when_path_is_escaping_even_though_sha256_is_valid, archive_report_complete_is_some_only_when_both_fields_are_valid}`; `meridian-app/src/validation/mechanical_integrity/sha_provenance.rs::tests::{a_source_archive_missing_path_or_sha256_is_a_new_incomplete_diagnostic, a_source_archive_with_a_malformed_but_present_sha256_still_attempts_the_read}`; реальный Node/Rust на ОДНОМ И ТОМ ЖЕ мутированном рабочем дереве (полная копия репозитория, `copyRepoWithoutGitOrTarget`, плюс один добавленный `skills/<name>/PIN.yaml` с `source_archive.path` без `source_archive.sha256`): `test/conformance-harness.test.mjs`, блок «sha-provenance: неполный source_archive» — Node не выводит ни OK, ни FAIL про source archive этого скилла вообще, Rust CLI сообщает РОВНО ОДИН `FAIL … missing path, sha256, or both` |
| `operating-foundation` (`meridian-cli-foundation-architecture-remediation`, первый корректирующий раунд, item 2; финализировано вторым корректирующим раундом, item 4) — data-запись без `id` больше не проходит молча | `comparePool` (`scripts/kernel-validate.mjs`) строит `data`/`duplicateData`/`undocumented` через `String(entry?.id ?? '')` и truthy-охраняемые фильтры (`id &&`) — запись без `id` (или с пустым `id`) молча выпадает из КАЖДОЙ из этих проверок и не получает никакой собственной диагностики | `meridian_core::mechanical_integrity::operating_foundation::FoundationEntry::new` отклоняет пустой `id` на построении (валидный доменный тип с приватными полями; `ru`/`en` НЕ отклоняются — четвёртый корректирующий раунд item 2 сохраняет их доступными для сравнения bilingual-имён) — приложение (`meridian-app/src/validation/mechanical_integrity/operating_foundation.rs::resolve_entries`) резолвит каждую сырую запись независимо, считает конструктивные отказы и сообщает РОВНО ОДИН FAIL (`entry_missing_id`, с точным числом) для всей партии, прежде чем передать доменному `check_pool` только успешно построенные записи — отказавшая запись никогда не достигает `check_pool` под видом валидного значения | Rust: `meridian-core/src/mechanical_integrity/operating_foundation.rs::tests::foundation_entry_rejects_empty_id`, `::entry_missing_id_names_the_label_and_count`; `meridian-app/src/validation/mechanical_integrity/operating_foundation.rs::tests::an_entry_with_no_id_is_its_own_reported_defect_not_silently_dropped` (остальные, корректные записи по-прежнему проверяются как обычно); реальный Node/Rust на ОДНОМ И ТОМ ЖЕ мутированном рабочем дереве (полная копия репозитория, `copyRepoWithoutGitOrTarget`, плюс одна добавленная запись `terms[]` без `id` в `standards/workspace/operating-foundation.yaml`): `test/conformance-harness.test.mjs`, блок «operating-foundation: entry без id» — Node не выводит ни один FAIL про эту запись (и ни одна другая term/principle запись не расходится), Rust CLI сообщает РОВНО ОДИН `FAIL … 1 term entry(ies) ... have no id` |
| `stack-profiles` (подпакет 7a, второй и третий корректирующие раунды, §5.5d п. 2; **принято архитектором** как сохраняемое отличие, план §5.16.3 п. 9; в реестр внесено квалификацией `rust-business-contract-qualification`) — `stack-profiles.profiles` неверного YAML-типа | `comparePool` (`scripts/kernel-validate.mjs`) вызывает `entries.map` на значении, которое не является списком: процесс завершается необработанным `TypeError: entries.map is not a function` с кодом 1, stdout пуст — `ok()`/`fail()`/`warn()` печатают накопленное только при штатном завершении, поэтому теряются диагностики всех уже пройденных проверок | `meridian_app::validation::mechanical_integrity::stack_profiles::run` отклоняет не-список на транспортной границе ровно одним авторским `FAIL stack-profiles: "profiles" must be a list, found a <тип>`, пул не загружается, а `validate` продолжается до полного результата: код 1 (`DOMAIN_NEGATIVE`), пустой stderr, один JSON-документ. **Бизнес-эффект:** вердикт не меняется (обе стороны отказываются от успешного кода, fail-open нет); вместо аварии, теряющей всю информацию, оператор получает полный отчёт с названной причиной | Rust: `meridian-app/src/validation/mechanical_integrity/stack_profiles.rs::tests::a_wrong_type_profiles_key_reports_and_does_not_load` (отрицательная граница) и `…::agreeing_halves_produce_no_diagnostics` (положительная); реальный Node/Rust на ОДНОМ И ТОМ ЖЕ мутированном рабочем дереве (`copyRepoWithoutGitOrTarget`, `stack-profiles/stack-profiles.yaml` = `schema_version: 1` / `profiles: true`): `test/conformance-harness.test.mjs`, проверки «намеренная граница: Node-эталон падает с необработанным TypeError на profiles неверного типа …» (код 1, `signal: null`, пустой stdout, `TypeError` на stderr) и «намеренная граница: реальный `meridian validate` на profiles неверного типа даёт явный authored FAIL и полный JSON-результат …» |
| `task-pattern-registry` / `task-specification-contract` (`rust-architecture-conformance-3`) — coupled pair: catalogue publication и зависимое разрешение `task_pattern`; итоговый registry-вердикт не меняется, различается ровно одна вторичная diagnostic, и только на уже `FAIL`-ящем Kernel (здоровый каталог этот путь никогда не задевает) | `task-specification-contract`-блок `scripts/kernel-validate.mjs` (~строка 1225) повторно читает и разбирает `standards/workspace/task-pattern-registry.yaml` НАПРЯМУЮ, независимо от того, успел ли ранее блок `task-pattern-registry` (`tprOk`); дефект каталога в одном месте (например дублирующийся `id`) никогда не мешает `task-specification-contract` разрешить `task_pattern`-ссылки, присутствующие в сыром списке | `meridian_app::operating_model::task_pattern_registry::evaluate` публикует `TaskPatternCatalog` только после того, как ВСЕ правила уровня каталога уже пройдены (`rust-architecture-conformance-3`, корректирующий раунд item 1: fail-closed `TaskPatternCatalog::build` — дублирующийся `id` где угодно даёт `catalog: None` целиком, не частично построенный каталог); `meridian-cli/src/commands/validate/task_specification.rs::run` при отсутствующем каталоге сообщает ровно одну зависимую диагностику `task-specification-contract: no task-pattern catalog is available; task-pattern-registry must be checked first`, не пытаясь читать рабочее пространство через `FsWorkspaceReader` вовсе | Rust: `meridian-cli/src/commands/validate/task_specification.rs::tests::no_catalog_fails_closed_without_touching_the_workspace`; `meridian-app/src/operating_model/task_pattern_registry.rs::tests::duplicate_pattern_id_is_reported_through_the_full_pipeline`, `::a_domain_construction_failure_leaves_the_catalog_none`; реальный Node/Rust на ОДНОМ И ТОМ ЖЕ мутированном рабочем дереве (полная копия репозитория, `copyRepoWithoutGitOrTarget`, существующая мутация `duplicateOneTaskPatternWithNewId` — дублирует один pattern под новым `id`, не используемым ни одной реальной спецификацией): `test/conformance-harness.test.mjs`, `assertTaskPatternRegistryMutationAcceptedDivergence` — `task-pattern-registry`-диагностики Node и Rust равны как multiset (одна и та же строка `pattern id "mutation-probe-pattern" is declared more than once`), Rust-дельта несёт РОВНО ОДНУ дополнительную строку `task-specification-contract: no task-pattern catalog is available; task-pattern-registry must be checked first`, Node-дельта её не несёт вовсе, и после удаления этой единственной строки Node- и Rust-дельты равны как multiset |
| `functional-parity` (`rust-architecture-conformance-4`) — граница 1 из 2: non-`NotFound` I/O при чтении самой PHASE D evidence-схемы (`verification/functional-parity/functional-parity-evidence.schema.json`) | `scripts/kernel-validate.mjs`'s `readIfExists` ловит ЛЮБУЮ ошибку `fs.readFileSync` схемы — `ENOENT` и любую другую (`EISDIR`, отказ в доступе, ошибка кодировки) — и возвращает `null`; блок `6c. functional-parity` целиком пропускается тем же `INFO functional-parity: no PHASE D evidence schema in this Kernel; nothing to check`, что и при обычном отсутствии схемы — ни одного FAIL | `meridian_app::operating_model::functional_parity::evaluate` сохраняет прежний тихий skip ТОЛЬКО для `ReadError::NotFound`; любая другая `ReadError::Io` даёт РОВНО ОДИН `FAIL functional-parity: functional-parity-evidence.schema.json: <сообщение ОС>`. **Бизнес-эффект:** вердикт меняется — Node проходит молча (skip), Rust падает (`FAIL`); нечитаемая, но существующая схема больше не выдаётся за «ещё не появившуюся» | Rust: `meridian-app/src/operating_model/functional_parity/mod.rs::tests::a_schema_io_error_other_than_not_found_fails_closed_instead_of_skipping` (ровно один `FAIL`) и `::a_missing_schema_produces_no_diagnostics` (позитивная граница: `NotFound` — ноль диагностик); реальный Node/Rust на ОДНОМ И ТОМ ЖЕ мутированном рабочем дереве (`copyRepoWithoutGitOrTarget`, файл схемы заменён каталогом — переносимый non-`ENOENT`): `test/conformance-harness.test.mjs`, блок `rust-architecture-conformance-4` schema-I/O — Node печатает `INFO … nothing to check` без FAIL, Rust CLI — ровно один `FAIL functional-parity: functional-parity-evidence.schema.json:` (OS-хвост не сравнивается) |
| `functional-parity` (`rust-architecture-conformance-4`) — граница 2 из 2: non-`NotFound` I/O при чтении fixtures (`verification/functional-parity/fixtures/functional-parity-evidence.fixtures.json`), когда схема уже прочитана | тот же `readIfExists` сворачивает ЛЮБУЮ ошибку чтения fixtures в `null`, и Node сообщает один и тот же generic `FAIL functional-parity: the PHASE D evidence schema carries no fixtures (…); a schema no run exercises is not one this gate has reached` — и для отсутствующего, и для существующего, но нечитаемого файла | Rust сохраняет прежний generic текст ТОЛЬКО для `ReadError::NotFound`; любая другая `ReadError::Io` даёт РОВНО ОДИН отдельный `FAIL functional-parity: verification/functional-parity/fixtures/functional-parity-evidence.fixtures.json could not be read: <сообщение ОС>`. **Бизнес-эффект:** уже-failing вердикт НЕ меняется (обе стороны `FAIL`); меняется только точный текст диагностики — оператор отличает «файл не создан» от «файл есть, но не читается» | Rust: `meridian-app/src/operating_model/functional_parity/mod.rs::tests::an_unreadable_fixtures_file_fails_closed_with_distinct_text_from_missing` (отдельный текст, без `carries no fixtures`) и `::a_missing_fixtures_file_fails_closed_with_the_not_found_text` (позитивная граница: `NotFound` — прежний generic текст); реальный Node/Rust на ОДНОМ И ТОМ ЖЕ мутированном рабочем дереве (`copyRepoWithoutGitOrTarget`, файл fixtures заменён каталогом): `test/conformance-harness.test.mjs`, блок `rust-architecture-conformance-4` fixture-I/O — Node: ровно один functional-parity FAIL с generic `carries no fixtures`; Rust: ровно один functional-parity FAIL с префиксом `…functional-parity-evidence.fixtures.json could not be read:`; обе стороны failing (OS-хвост не сравнивается) |
| `execution-state-model`, `role-and-human-control`, `bounded-context-manifest` (`rust-architecture-conformance-5`) — non-`NotFound` I/O при чтении ЛЮБОГО из обязательных файлов пакета: `registries/operating-model/{execution-state,role-registry,human-control,context-manifest,scoped-record}.schema.json`, три fixture bundle в `registries/operating-model/fixtures/` и канонический `standards/workspace/role-registry.yaml` | `readIfExists` (`scripts/kernel-validate.mjs`) сворачивает ЛЮБУЮ ошибку чтения (`ENOENT`, `EISDIR`, отказ в доступе) в `null`, и Node сообщает для существующего, но нечитаемого файла тот же текст, что и для отсутствующего: `… is missing; …` для схем, envelope и каталога, `… carries no fixtures (…)` для bundle | Все файлы обязательны, обе ветви fail-closed: `ReadError::NotFound` сохраняет прежний Node-текст дословно; любая другая `ReadError::Io` даёт РОВНО ОДИН отдельный `FAIL <family>: <workspace-путь> could not be read: <сообщение ОС>` (`meridian-app/src/operating_model/run_contract_boundary/mod.rs::unreadable`), после которого семейство останавливается в той же точке, что и при отсутствии файла. Каталог на месте файла через реальный `FsWorkspaceReader` — `Io`, не «отсутствует». **Бизнес-эффект:** вердикт НЕ меняется (обе стороны `FAIL`); меняется только текст — оператор отличает «файл не создан» от «файл есть, но не читается» | Rust (fake reader, положительная и отрицательная граница на каждый вид файла): `meridian-app/src/operating_model/execution_state/mod.rs::tests::{a_missing_schema_keeps_the_node_text_and_an_unreadable_one_is_distinct, a_missing_or_unreadable_envelope_fails_closed_before_any_fixture, missing_and_unreadable_fixtures_are_distinct_failures}`, `role_and_human_control/mod.rs::tests::missing_and_unreadable_mandatory_files_are_distinct_failures`, `bounded_context_manifest/mod.rs::tests::missing_and_unreadable_mandatory_files_are_distinct_failures`; Rust (реальный адаптер): `meridian-cli/src/commands/validate/mod.rs::rust_architecture_conformance_5::{a_directory_in_place_of_a_mandatory_file_is_unreadable_not_missing, an_app_generated_failure_carries_exactly_one_family_prefix}`; реальный Node/Rust на ОДНОМ И ТОМ ЖЕ мутированном рабочем дереве (`copyRepoWithoutGitOrTarget`; `execution-state.schema.json`, `role-registry.yaml` и `context-manifest.fixtures.json` заменены каталогами): `test/conformance-harness.test.mjs`, блок `rust-architecture-conformance-5` mandatory-read I/O — Node: по одному FAIL на семейство с прежним `is missing`/`carries no fixtures`; Rust: по одному FAIL на семейство с префиксом `<family>: <путь> could not be read:`; обе стороны failing (OS-хвост не сравнивается) |
| `bounded-context-manifest` (подпакет 7b, корректирующий раунд, item 2; перенесено без изменений в `rust-architecture-conformance-5`), с `rust-architecture-conformance-6` также `evidence-and-handoff-contract` и `meridian-field-evaluation` — порядок нескольких неизвестных полей одного ответа резолвера | `Object.keys()` ответа резолвера (`scripts/lib/context-manifest.mjs`) перечисляет неизвестные поля в порядке исходного текста JSON, поэтому первым (`p[0]`) называется первое по тексту | Типизированный `meridian_core::run_contracts::ResolvedEntry` несёт неизвестные ключи ответа и его `resolved_state` по имени в отсортированном порядке (единая конверсия трёх семейств `meridian-app/src/operating_model/record_resolution.rs`), и core (`meridian_core::run_contracts::resolution::check_resolved_entry`, один владелец закрытого контракта ответа для трёх семейств) сообщает их в этом детерминированном порядке, не зависящем от исходного текста. **Бизнес-эффект:** вердикт не меняется (обе стороны fail-closed на том же дефекте), различается только то, какое из неизвестных полей названо первым | Rust: `meridian-app/src/operating_model/bounded_context_manifest/mod.rs::tests::unknown_resolver_fields_are_reported_in_stable_sorted_order`, `meridian-app/src/operating_model/record_resolution.rs::tests::known_fields_are_typed_and_foreign_kinds_are_kept_for_the_diagnostic`, `meridian-app/src/operating_model/evidence_and_handoff/tests.rs::evidence_and_handoff_unknown_resolver_fields_are_reported_in_stable_sorted_order`; реальный Node/Rust: `test/conformance-harness.test.mjs`, блок «unknown-field-order» (подпакет 7b) — Node-дельта называет `zzzz_extra_field`, Rust-дельта — `aaaa_extra_field`, после замены имени дельты равны как multiset |
| `evidence-and-handoff-contract`, `meridian-field-evaluation` (`rust-architecture-conformance-6`) — non-`NotFound` I/O при чтении ЛЮБОГО из обязательных файлов двух семейств: `registries/operating-model/{evidence-and-handoff,field-evaluation,scoped-record}.schema.json` и два fixture bundle в `registries/operating-model/fixtures/` | `readIfExists` (`scripts/kernel-validate.mjs`) сворачивает ЛЮБУЮ ошибку чтения (`ENOENT`, `EISDIR`, отказ в доступе) в `null`, и Node сообщает для существующего, но нечитаемого файла тот же текст, что и для отсутствующего: `… is missing; …` для схем и envelope, `… carries no fixtures (…)` для bundle | То же, заранее принятое пакетами 4–5 различение, расширенное на два семейства: все файлы обязательны, обе ветви fail-closed; `ReadError::NotFound` сохраняет прежний Node-текст дословно, любая другая `ReadError::Io` даёт РОВНО ОДИН отдельный `FAIL <family>: <workspace-путь> could not be read: <сообщение ОС>` (общий загрузчик `meridian-app/src/operating_model/run_contract_boundary/mod.rs::load_resolving_bundle`), после чего семейство останавливается в той же точке, что и при отсутствии файла. **Бизнес-эффект:** вердикт НЕ меняется (обе стороны `FAIL`); меняется только текст | Rust (fake reader, положительная и отрицательная граница для схемы, envelope и bundle каждого семейства): `meridian-app/src/operating_model/evidence_and_handoff/tests.rs::evidence_and_handoff_missing_and_unreadable_mandatory_files_are_distinct_failures`, `meridian-app/src/operating_model/field_evaluation/tests.rs::field_evaluation_missing_and_unreadable_mandatory_files_are_distinct_failures`; Rust (реальный адаптер): `meridian-cli/src/commands/validate/mod.rs::rust_architecture_conformance_6::{evidence_and_handoff_and_field_evaluation_unreadable_is_not_missing, evidence_and_handoff_and_field_evaluation_failures_carry_exactly_one_prefix}`; реальный Node/Rust на ОДНОМ И ТОМ ЖЕ мутированном рабочем дереве (`copyRepoWithoutGitOrTarget`; fixture bundle handoff и схема field evaluation заменены каталогами): `test/conformance-harness.test.mjs`, блок `rust-architecture-conformance-6` mandatory-read I/O — Node: по одному FAIL с прежним `carries no fixtures`/`is missing`; Rust: по одному FAIL с префиксом `<family>: <путь> could not be read:`; обе стороны failing (OS-хвост не сравнивается) |
| `evidence-and-handoff-contract`, `meridian-field-evaluation` (`rust-architecture-conformance-6`, §5.20.3 п. 14) — **библиотечное, не CLI-наблюдаемое** отличие: schema short-circuit закрытого DTO | `evaluateEvidenceAndHandoff` / `evaluateFieldEvaluation` не останавливаются на нарушении схемы (только на исключении её применения) и продолжают доменный анализ записи, уже нарушившей схему | Маршрут `schema gate → закрытый DTO → typed input → core` останавливается на schema gate: схема-невалидная запись даёт только диагностики схемы (`meridian-app/src/operating_model/run_contract_boundary/mod.rs::evaluate_record`, тот же прецедент, что принят для `controlled-rule-intake`). Первая диагностика у обеих сторон — диагностика схемы, поэтому CLI (`validate` сообщает для отклонённой valid-fixture только первую проблему, для invalid-fixture — только факт отказа) это различие не показывает; на 95+36 реальных invalid fixtures отличаются только вторичные доменные диагностики после схемных. **Бизнес-эффект:** вердикт записи не меняется | Прямой библиотечный matched case на ОДНОМ И ТОМ ЖЕ реальном документе с ОДИНАКОВЫМИ двумя мутациями — дефект, который ловит ТОЛЬКО схема (повторный `covers` против `uniqueItems`; `observed_at` против шаблона `date_str` — закрытый DTO принимает оба), и независимый доменный дефект: Rust — `meridian-app/src/operating_model/evidence_and_handoff/tests.rs::evidence_and_handoff_a_schema_violation_short_circuits_before_the_domain`, `meridian-app/src/operating_model/field_evaluation/tests.rs::field_evaluation_a_schema_violation_short_circuits_before_the_domain` (только диагностики схемы); Node — `test/conformance-harness.test.mjs`, блок `rust-architecture-conformance-6` schema short-circuit (прямой импорт обеих функций: диагностика схемы первой И доменные диагностики) |
| `meridian-field-evaluation` (`rust-architecture-conformance-6`) — **библиотечное, не CLI-наблюдаемое** сужение, **принято архитектором** (корректирующий раунд пакета 6) как Rust-native граница: наблюдение, отвергнутое закрытым контрактом measurement или обязательного окна, не участвует в пересчёте aggregate/status | `evaluateReportBody` сообщает дефекты разрешённого наблюдения (`resolvedObservationIssues`), но затем всё равно относит его к выборке метрики по сырым значениям: `observed` без measurement-объекта считается «прочим» (влияет на ожидаемый статус), отсутствующее окно `post-acceptance-defects` — «без окна», а `computeMetricAggregate` суммирует отвергнутые значения JS-приведением; отсюда вторичные диагностики статуса/агрегата | Агрегат и статус пересчитываются только из типизированных образцов (`meridian_core::field_evaluation::resolved::contribution`): сначала обрабатывается `status`; при `status = "observed"` любое `measurement`, кроме `Present(Some(поля))`, прошедшего закрытый контракт измерения, даёт `NotComputable`; для метрики с обязательным окном любое `observation_period`, кроме `Present(Some(окно))` с существующими датами в порядке и допустимым `coverage`, даёт `NotComputable`. Для метрики с таким наблюдением пересчёт статуса и агрегата не выполняется — дефект наблюдения уже сообщён и отклоняет отчёт; отвергнутые значения никогда не становятся образцом; остальные метрики проверяются полностью. **Бизнес-эффект:** вердикт отчёта не меняется (отклонён на обеих сторонах той же первой диагностикой разрешённого наблюдения); отсутствуют только вторичные диагностики, вычисленные Node из отвергнутых значений | Rust (реальный `check_field_evaluation`, отчёт целиком): `meridian-core/src/field_evaluation/tests.rs::field_evaluation_a_refused_resolved_measurement_or_window_is_never_aggregated` — для каждой границы ровно одна диагностика разрешённого наблюдения и ни одной вторичной диагностики статуса/агрегата: отсутствующее measurement при `observed`; `null` measurement; не-объектное measurement; невалидное `measurement.seconds`; отсутствующее обязательное `observation_period`; `null` обязательное `observation_period`; не-объектное обязательное `observation_period` (тест проверен на зубы: при прежней логике `contribution` падают случаи measurement и окна); положительная граница — `field_evaluation_a_report_is_accepted_with_eight_recomputed_aggregates` и `field_evaluation_a_stated_aggregate_or_status_that_diverges_is_rejected` (при типизированных образцах расхождение агрегата и статуса сообщается) |
| `instance-data-migration`, `instance-canonical-export`, `workspace-compatibility-qualification`, `upgrade-integration-qualification` (`rust-architecture-conformance-7`) — non-`NotFound` I/O при чтении ЛЮБОГО из обязательных файлов (схема семейства, каждая композируемая схема, fixture bundle); заранее принятый класс пакетов 4–6 | `readIfExists` возвращает `null` на любую ошибку чтения, и нечитаемый файл сообщается как отсутствующий (`… is missing; …` / `the schema carries no fixtures …`) | `ReadError::NotFound` сохраняет текст Node; любая иная `ReadError::Io` — отдельный fail-closed `<path> could not be read: <ошибка>`; обе ветви `Fail` | fake reader: `meridian-app/src/operating_model/instance_data_migration/tests.rs::instance_data_migration_a_missing_or_unreadable_mandatory_file_fails_closed_and_is_distinguished`, `meridian-app/src/operating_model/instance_canonical_export/tests.rs::instance_canonical_export_missing_malformed_and_unreadable_files_fail_closed`, `meridian-app/src/operating_model/workspace_compatibility_qualification/tests.rs::workspace_compatibility_qualification_every_composed_schema_is_mandatory`, `meridian-app/src/operating_model/upgrade_integration_qualification/tests.rs::upgrade_integration_qualification_every_composed_schema_is_mandatory`; реальный `FsWorkspaceReader`: `meridian-cli/src/commands/validate/rust_architecture_conformance_7.rs::migration_and_qualification_unreadable_is_not_missing` |
| `instance-data-migration`, `instance-canonical-export`, `workspace-compatibility-qualification`, `upgrade-integration-qualification` (`rust-architecture-conformance-7`) — **библиотечное, не CLI-наблюдаемое** отличие, **принято архитектором** (корректирующий раунд 1 пакета 7) как тот же Rust-native класс, что приняты для пакетов 1 и 6: schema short-circuit контейнера и envelope | `evaluateInstanceDataMigration` / `evaluateInstanceCanonicalExport` / `evaluateWorkspaceCompatibilityQualification` / `evaluateUpgradeIntegrationQualification` не останавливаются на ошибках схемы контейнера или envelope записи и продолжают предметные проверки документа, уже нарушившего схему | Маршрут `container gate → закрытый DTO → typed input → core` (`meridian-app/src/operating_model/migration_boundary/mod.rs::typed_container`) останавливается на schema gate: контейнер, не прошедший схему семейства, или контейнер, в котором envelope хотя бы одной записи не прошёл `scoped-record.schema.json`, даёт только диагностики схемы и не доходит до DTO и core. Первая диагностика у обеих сторон — диагностика схемы, поэтому `validate` (для отклонённой valid-fixture — только первая проблема, для invalid-fixture — только факт отказа) это различие не показывает. **Бизнес-эффект:** вердикт записи и CLI-вывод не меняются; отсутствуют только вторичные доменные диагностики документа, уже отвергнутого схемой | Прямой библиотечный matched case для КАЖДОГО из четырёх семейств на ОДНОМ И ТОМ ЖЕ реальном valid-контейнере с ОДИНАКОВЫМИ мутациями — независимый schema-clean доменный дефект (план объявляет `supersedes` на себя; чужой корректный `idempotency_key` экспорта; подменённый `origin.source_ref` квалификации; blocker у `QUALIFIED`) и schema-only дефект, который закрытый DTO принял бы (пустой `title`: `minLength: 1`), отдельно на контейнере и на envelope первой записи. Rust — `meridian-app/src/operating_model/{instance_data_migration,instance_canonical_export,workspace_compatibility_qualification,upgrade_integration_qualification}/tests.rs::{instance_data_migration_a_container_or_envelope_schema_violation_short_circuits_before_the_domain, instance_canonical_export_a_container_or_envelope_schema_violation_short_circuits_before_the_domain, workspace_compatibility_qualification_a_container_or_envelope_schema_violation_short_circuits_before_the_domain, upgrade_integration_qualification_a_container_or_envelope_schema_violation_short_circuits_before_the_domain}` соответственно (общий `migration_boundary/test_support.rs::assert_schema_short_circuit`): только доменный дефект — ровно `[доменная строка]` (`DomainRejected`); с schema-only дефектом — ровно `["/title: shorter than 1"]` / `["entry 0 envelope /title: shorter than 1"]` (`SchemaRejected`). Node — `test/conformance-harness.test.mjs`, блок `rust-architecture-conformance-7`, корректирующий раунд 1, schema short-circuit: только доменный дефект — ровно `[доменная строка]`; с schema-only дефектом — ровно `[диагностика схемы, доменная строка]`. Точная граница различия — одна доменная строка после схемной |
| `migration` plan fingerprint (`rust-architecture-conformance-7`) — **сближение**, не расхождение: исправлен дефект прежнего Rust owner | `computePlanFingerprint` сортирует `mappings` по `` `${unit_id}::${target.id}`.localeCompare `` (ICU: `":"` раньше цифр) | прежний Rust сортировал по code unit и для `unit-1`/`unit-10` давал иной fingerprint; теперь `meridian_core::canonical::locale_order` воспроизводит ICU-порядок для алфавита semantic id | `meridian-core/tests/migration_fingerprint_reference.rs::plan_fingerprint_sorts_mapping_keys_in_the_reference_locale_order` (эталонный хеш получен реальным Node), `meridian-core/src/canonical.rs::tests::locale_order_matches_the_node_reference_for_the_semantic_id_alphabet` |
| `checkOpaqueRef` / `meridian_core::types::evidence_ref::opaque_ref_fault` (`rust-architecture-conformance-7`) — **сближение**, не расхождение: устранён panic прежнего Rust | `checkOpaqueRef` проверяет префикс `file://` регулярным выражением и для ref с многобайтным символом на границе седьмого байта (например `abcdefж/record`) возвращает обычный результат (`null` — ref допустим) | Прежний Rust брал срез `value[..7]` по байтам и паниковал, если седьмой байт попадал внутрь многобайтного UTF-8 символа; теперь `value.get(..7)` — такой ref не является префиксом `file://` и проверяется дальше обычными правилами. **Бизнес-эффект:** аварийное завершение на допустимом ref устранено; Node и Rust возвращают одинаковый обычный результат | Rust: `meridian-core/src/types/evidence_ref.rs::tests::a_multibyte_prefix_is_checked_without_panicking` (multibyte regression: `abcdefж/record`, `ффф:x`); Node: `test/conformance-harness.test.mjs`, блок `rust-architecture-conformance-7`, корректирующий раунд 1, сближение opaque-ref (`checkOpaqueRef` возвращает `null` для тех же двух значений) |
| `workspace-compatibility-qualification`, `upgrade-integration-qualification` (`rust-architecture-conformance-7`) — **принято архитектором** (корректирующий раунд 1 пакета 7): решающие входы и вторичные проверки квалификации читаются только из композируемых записей, ПРИНЯТЫХ их собственным production-check | Node читает `scope`, `payload.repository.id`, `next_step` и `rule_candidates` разрешённого соединения, `outcome.status`/`scope.workspace_id` task journey, `payload.workspace_id` field-evaluation report и `scope`/`workspace_id` scenario-записей из СЫРОЙ разрешённой записи — даже если её собственная композиция вернула проблемы | Отвергнутый собственным контрактом raw JSON не участвует в scope-, workspace-, repository-set- и decision-matrix-проверках: `next_step`/кандидаты отклонённого соединения — fail-closed «неизвестно» (матрица → `UNVERIFIED`); scope/workspace-сверка отклонённой записи не выполняется; «объявлен, но не просканирован» проверяется только когда приняты ВСЕ соединения (`meridian-core/src/qualification/{workspace,upgrade}.rs`). Собственные диагностики отклонённой записи сообщаются обеими сторонами одинаково. **Бизнес-эффект:** вердикт квалификации не меняется — запись отклоняется обеими сторонами (её композиция уже сообщила проблему); принятые квалификации не меняются (в них все композируемые записи приняты). Меняется только состав вторичных диагностик уже отклонённой квалификации (исчезают строки, вычисленные Node из отвергнутых значений; может появиться строка матрицы `UNVERIFIED`) и — только если отклонённое соединение одновременно расходится по scope — текст первой строки, единственный видимый `validate` для отклонённой valid-fixture | Прямой библиотечный matched case на ОДНОМ И ТОМ ЖЕ реальном документе с ОДИНАКОВОЙ мутацией, перепиненной на один и тот же пересчитанный digest (значение утверждается обеими сторонами). Workspace: branch-5 fixture (`QUALIFIED`), у соединения `sample-connection-clean` изменены `scope.id` и `payload.repository.id` — собственный контракт его отклоняет; Rust — `meridian-app/src/operating_model/workspace_compatibility_qualification/tests.rs::workspace_compatibility_qualification_a_rejected_connection_contributes_no_raw_decision_input` (ровно `[диагностика композиции, строка матрицы UNVERIFIED]`); Node — `test/conformance-harness.test.mjs`, блок `rust-architecture-conformance-7`, корректирующий раунд 1: ровно `[raw scope, диагностика композиции, две raw repository-set строки]` — первая строка различается, общая ровно одна строка композиции. Upgrade: valid[0] (`QUALIFIED`), у field-evaluation report изменён `payload.workspace_id` — собственный контракт его отклоняет; Rust — `meridian-app/src/operating_model/upgrade_integration_qualification/tests.rs::upgrade_integration_qualification_a_rejected_report_contributes_no_raw_workspace` (ровно 18 строк композиции, строка пересчёта `BLOCKED`, строка blockers); Node — тот же блок harness: первая строка совпадает, Node-вывод = Rust-вывод плюс ровно одна raw-строка workspace identity. Core: `meridian-core/src/qualification/tests.rs::{qualification_workspace_a_rejected_plan_does_not_hide_its_neighbours, qualification_workspace_matrix_is_order_independent_and_fails_closed}` |
| `workspace-compatibility-qualification` (`rust-architecture-conformance-7`) — **принято архитектором** (корректирующий раунд 1 пакета 7): schema-invalid композируемый план/экспорт не получает fingerprint/digest из сырого JSON | Композируемый план/экспорт, не прошедший собственную схему, пинуется `computePlanFingerprint`/`computeExportDigest` над СЫРЫМ JSON: лишнее или неверное поле внутри проекции меняет пересчитанное значение, и Node сообщает только `payload.migration_plan_ref.sha256 … does not equal …` / `payload.canonical_export_ref.sha256 … does not equal …`, не показывая, что запись нарушает собственную схему | Fingerprint/digest имеют одного владельца — типизированную проекцию (`meridian_core::migration::projection`); пиннинг (`PinnedPlan::confirm`/`PinnedExport::confirm`) начинается только после построения typed schema-shaped input (`instance_data_migration::typed_plan`/`instance_canonical_export::typed_export`). Запись без такого input сообщается собственной диагностикой схемы своего маршрута под префиксом `migration_plan_ref: `/`canonical_export_ref: ` и не пинуется; дальнейшая композиция (экспорт без плана, матрица) — как у Node. **Бизнес-эффект:** вердикт квалификации не меняется (отклонена обеими сторонами, остальные строки совпадают); меняется только текст диагностики самой записи — вместо raw sha256-несовпадения называется фактическое нарушение схемы. `validate` видит это только как текст первой строки для отклонённой valid-fixture | Прямой библиотечный matched case на ОДНОМ И ТОМ ЖЕ реальном branch-9 документе (план + экспорт, `QUALIFIED`) с ОДИНАКОВОЙ мутацией — `payload.source.unexpected_field` у разрешённого плана, отдельно у разрешённого экспорта. Rust — `meridian-app/src/operating_model/workspace_compatibility_qualification/tests.rs::{workspace_compatibility_qualification_a_schema_invalid_composed_plan_is_never_pinned_by_raw_json, workspace_compatibility_qualification_a_schema_invalid_composed_export_is_never_pinned_by_raw_json}`: ровно `[<префикс>: /…/payload/source/unexpected_field: additional property not allowed, вторая строка]`, ни одной строки `sha256`. Node — `test/conformance-harness.test.mjs`, блок `rust-architecture-conformance-7`, корректирующий раунд 1, schema-invalid composition: ровно две строки, первая — raw `sha256 … does not equal …`, вторая совпадает с Rust; собственный Node-маршрут записи первой сообщает ту же диагностику схемы, что Rust. Композиционная точка: `meridian-app/src/operating_model/instance_data_migration/tests.rs::instance_data_migration_the_composition_point_types_one_resolved_plan` |
| `upgrade-integration-qualification` (`rust-architecture-conformance-7`) — **принято архитектором** (корректирующий раунд 1 пакета 7) как расширение принятой границы `rust-architecture-conformance-3` на второго потребителя каталога; наблюдаемо только на уже `FAIL`-ящем Kernel | блок `upgrade-integration-qualification` сам повторно разбирает `task-pattern-registry.yaml`; дефект каталога его не затрагивает | семейство композирует спецификации против ОДНОГО опубликованного `TaskPatternCatalog`; при `catalog: None` сообщает ровно одну зависимую строку `upgrade-integration-qualification: no task-pattern catalog is available; task-pattern-registry must be checked first`, не читая рабочее пространство. Семейство использует тот же опубликованный `TaskPatternCatalog`, что `task-specification-contract`, без второго разбора YAML. **Бизнес-эффект:** вердикт Kernel не меняется (он уже `FAIL` из-за дефекта каталога); добавляется ровно одна зависимая строка, называющая причину | `meridian-cli/src/commands/validate/rust_architecture_conformance_7.rs::upgrade_integration_qualification_without_a_catalog_fails_closed`; `test/conformance-harness.test.mjs`, `assertTaskPatternRegistryMutationAcceptedDivergence` (Rust-дельта несёт ровно по одной зависимой строке двух семейств) |
| `instance-data-migration`, `instance-canonical-export` (`rust-architecture-conformance-7`) — **принято архитектором** (корректирующий раунд 1 пакета 7): нейтральный хвост сообщения о невалидном JSON content | `content is not valid JSON for media_type "…": ${e.message}` — хвост после `: ` — текст ошибки парсера V8 (например `Expected property name or '}' in JSON at position 1 (line 1 column 2)`), зависящий от движка и версии | Та же проверка в том же месте порядка; хвост — нейтральный `the content does not parse as a single JSON value` (`meridian-core/src/migration/content.rs`). **Бизнес-эффект:** вердикт, набор, порядок и диагностический контекст (запись, цель, `media_type`) не меняются; отличается только хвост одной строки после `media_type "…": ` | Прямой библиотечный matched case на реальных документах: `instance-canonical-export` invalid[21] без изменений («planted an exported record whose application/json payload content is not valid JSON») и `instance-data-migration` valid[1] с `content` первой цели = `{not valid json`. Rust — `meridian-app/src/operating_model/instance_canonical_export/tests.rs::instance_canonical_export_invalid_json_content_is_rejected_like_the_reference`, `meridian-app/src/operating_model/instance_data_migration/tests.rs::instance_data_migration_invalid_json_content_is_rejected_like_the_reference` (точные 3 и 5 строк); Node — `test/conformance-harness.test.mjs`, блок `rust-architecture-conformance-7`, корректирующий раунд 1, invalid JSON content: то же число строк, в том же порядке, все строки совпадают с Rust дословно, кроме ровно одной invalid-JSON строки, совпадающей до `media_type "application/json": ` и имеющей непустой хвост парсера. Юнит: `meridian-core/src/migration/content.rs::tests::json_content_is_parsed_then_checked_for_canonical_form` |
| `meridian validate` (`rust-architecture-conformance-7`, §5.21.3 п. 12) — удаление пустого blocker-механизма | не применимо (у Node нет механизма блокировки) | после подключения четырёх семейств 7d удалены `BLOCKED_CHECKS`, поле `result.blocked`, хвост `… check(s) blocked pending …` итоговой строки и вердикт `BLOCKED`; чистый Kernel даёт `ok`/код `0`, совпадая с Node | `meridian-cli/tests/binary_runs.rs::validate_reports_ok_on_this_kernel_with_zero_real_failures`; `test/conformance-harness.test.mjs`, `real-node-rust-cli-validate-clean-kernel` (`conformant`) |
| `meridian import`/`migration verify` — доказательство применимости (`meridian-cli-migration`, §5.22.4 п. 12) — **принято архитектором** (вердикт первой передачи пакета 8) | `migration/instance-data/verify.mjs` замороженного источника сравнивает 61 норму по проекции `{scope, activation, status, repository, technology_profile, globs}` — `activation` только строкой, `product_domain` и `task_class` не сравниваются | `meridian_core::migration::applicability::compare_applicability` сравнивает типизированные `ApplicabilityRecord` (оба края прошли тот же Kernel-контракт `applicability.schema.json` + DTO резолвера): полный `ApplicabilityScope` (включая `product_domain`), полный `Activation` (включая селектор `task_class`, `globs` без учёта порядка) и вид `status`; идентичность — та же 7-польная, что у эталона. На реальном bundle обе стороны дают 61 норму, lost/added/changed/duplicate = 0 | Rust: `meridian-core/src/migration/applicability.rs::tests::{migration_applicability_identical_sides_are_equivalent, migration_applicability_detects_lost_added_changed_and_duplicate, migration_applicability_empty_sides_are_never_equivalent, migration_applicability_a_re_resolution_on_another_date_is_its_own_identity}`; реальный Node/Rust: `test/conformance-harness.test.mjs`, проверка «meridian-cli-migration: эквивалентность применимости» |
| `meridian import --kind frozen-instance`/`migration plan` — восстановление фактического содержимого единиц (`meridian-cli-migration`, §5.22.4 п. 6–7) — **принято архитектором** (вердикт первой передачи пакета 8) | `verify.mjs` держит таблицу из 20 путей-контейнеров конкретного продуктового Instance с ручными правилами адресации (`id`, `alias`, `application_id`, `repository_id`, позиция, `line/N`) | Kernel не содержит ни одного продуктового пути: `meridian-app/src/migration/fragment.rs` выводит адресацию элемента из самого закреплённого документа и объявленных соседних сегментов — единственная идентичность (позиция 0/1-based или скалярное поле каждого элемента), взаимно-однозначная с объявленными фрагментами; две расходящиеся идентичности, отсутствие идентичности или необъявленный член коллекции — отказ, а не догадка. На реальном bundle все 336 содержимых восстановлены из `git`-объектов закреплённой ревизии и побайтно совпали с принятым экспортом; `merged`-группы (0 в bundle) закрыто отклоняются | Rust: `meridian-app/src/migration/fragment/tests.rs::{migration_fragment_elements_are_addressed_by_their_one_identity, migration_fragment_jsonl_lines_and_map_members_are_addressed, migration_fragment_whole_carrier_keeps_its_exact_bytes, migration_fragment_segments_are_encoded_like_encode_uri_component, migration_fragment_an_applicability_register_is_recognised_by_its_own_schema, migration_fragment_ambiguous_identities_fail_closed, migration_fragment_an_undeclared_member_fails_the_whole_collection, migration_fragment_missing_carriers_and_mixed_declarations_fail_per_unit}`; binary: `meridian-cli/tests/binary_runs/migration.rs::migration_plan_fails_closed_on_every_foreign_source_state` (вариант `content-mismatch`); реальный bundle: `migration_real_bundle_*` (`--ignored`) и харнесс «meridian-cli-migration» |
| Хвост сообщения о невалидном JSON во всех маршрутах `meridian validate`, читающих JSON Kernel (`rust-business-contract-qualification`, GAP-10) — **принято архитектором** как класс | ``fail(`<семейство>: <файл> is not valid JSON: ${e.message}`)`` в 49 местах `scripts/kernel-validate.mjs`: хвост — текст `JSON.parse` движка V8 (`Expected double-quoted property name in JSON at position 18 (line 1 column 19)`), зависящий от версии движка | Контракт — код завершения, вердикт `FAIL` и авторский префикс `<семейство>: <файл> is not valid JSON: ` — совпадают; хвост — текст `serde_json` (`trailing comma at line 1 column 19`) и контрактом не является. Маршруты: `meridian-cli` `commands::validate::{registry_schema, rule_resolution_fixtures, instruction_source_registry, controlled_rule_intake, existing_project_compatibility_mode}`; `meridian-app` `operating_model::{functional_parity, task_pattern_registry, task_specification}` и `operating_model::run_contract_boundary::parse_json` (напрямую или через `migration_boundary`: `execution-state-model`, `role-and-human-control`, `bounded-context-manifest`, `evidence-and-handoff-contract`, `meridian-field-evaluation`, `instance-data-migration`, `instance-canonical-export`, `workspace-compatibility-qualification`, `upgrade-integration-qualification`). Нейтральный хвост невалидного JSON `content` миграции — отдельная строка выше, не этот класс | Положительный: `verification/conformance-harness/fixtures/conformance-harness.fixtures.json` `real-node-rust-cli-validate-clean-kernel` (те же маршруты на валидном JSON: равные `FAIL`/`WARN`). Отрицательный — каждый самостоятельный владелец парсера, реальные Node и `meridian`, `test/conformance-harness.test.mjs`: «accepted-rust-native: GAP-10 invalid-JSON tail: combined routes» (одна копия дерева с невалидными схемой общего прохода `$schema`, фикстурами `rule-resolution` и `functional-parity` и `execution-state.schema.json` — пять пар `FAIL`, включая зависимую строку `upgrade-integration-qualification`) и пять изолированных копий «… : <фикстуры> fixtures» для `instruction-source-registry`, `controlled-rule-intake`, `existing-project-compatibility-mode`, `task-pattern-registry`, `task-specification` (по одной паре). Итого 10 пар `FAIL`: в каждом case код 1 на обеих сторонах, равные `WARN`, авторские префиксы зафиксированы поимённо и равны, различается только хвост парсера, необъявленных `FAIL` нет. Единственные дополнительные строки — две Rust-only строки каскада при невалидных фикстурах `task-pattern-registry` («no task-pattern catalog is available» для `task-specification-contract` и `upgrade-integration-qualification`) — принадлежат уже принятым выше границам «каталог публикуется только целиком» и объявлены в case точным текстом; Rust: `meridian-app/src/operating_model/instance_data_migration/tests.rs::instance_data_migration_a_malformed_schema_or_bundle_fails_closed` (префикс) |
| Общий проход `schema`: `$schema`, который ни на что не указывает (`rust-business-contract-qualification`, GAP-11) — **принято архитектором** | `schema: <файл> references <ref>, which resolves to no file in the Instance or the Kernel` — Node ищет схему и в Instance | `schema: <файл> references <ref>, which resolves to no file in the Kernel`: штатная Rust-команда не читает Instance (план §6.5a.5), и текст называет только место, где маршрут действительно искал. Вердикт и код равны | Положительный: `meridian-cli/tests/binary_runs.rs::validate_reports_ok_on_this_kernel_with_zero_real_failures` (13/13 ссылок `$schema` реального Kernel разрешены). Отрицательный: `meridian-cli/tests/binary_runs.rs::validate_reports_a_missing_schema_as_absent_from_the_kernel_only` (реальный бинарник, без `MERIDIAN_INSTANCE`, точный текст); `test/conformance-harness.test.mjs` «accepted-rust-native: GAP-11 missing schema names the Kernel only» (реальные Node и Rust: единственное расхождение — эта пара строк) |
| Подмножество YAML: текст, который Node принимает с молчаливой потерей данных (`rust-business-contract-qualification`, GAP-12) — **принято архитектором**, fail-closed | `yamlParse` (`scripts/lib/yaml.mjs`) принимает `a: "unterminated` как строку с кавычкой, отбрасывает ключ с неверным отступом после блочной последовательности (`a:\n  - b\n c: d` → `{"a":["b"]}`) и ключ после корневой последовательности (`- a\nb: 1` → `["a"]`); документ с `$schema` затем проходит валидацию усечённым | Строгий слой RFC D-A сначала применяет собственные lint и reader подмножества, затем библиотеку: все три входа отклоняются (`schema: cannot parse <файл>: malformed YAML: …`), код 1. Rust никогда не валидирует и не публикует данные, которых нет в исходном тексте | Положительный: `meridian-app/src/source_format/yaml.rs::tests::parses_block_and_flow_subset`; контрольный документ в том же case харнесса принимается обеими сторонами. Отрицательный: `meridian-app/src/source_format/yaml.rs::tests::text_that_is_not_yaml_is_still_rejected_by_the_library`; `test/conformance-harness.test.mjs` «accepted-rust-native: GAP-12 lossy YAML is rejected fail-closed» (реальный Node-разбор трёх входов с потерей данных зафиксирован; у Rust ровно три `FAIL schema: cannot parse … malformed YAML:`, у Node — ни одного, остальное равно) |
| `instruction-topics`: `topics` неверного YAML-типа (`rust-business-contract-qualification`, GAP-13) — **принято архитектором** | `((intermediate value) \|\| []).map is not a function` — текст исключения движка внутри `try/catch` проверки, не авторская диагностика | Один авторский `FAIL instruction-topics: "topics" must be a list, found a boolean`; код и остальные диагностики равны | Положительный: `meridian-app/src/validation/mechanical_integrity/instruction_topics.rs::tests::the_real_kernel_pool_agrees_with_itself`. Отрицательный: `…::instruction_topics.rs::tests::a_wrong_type_topics_key_is_a_failure`; `test/conformance-harness.test.mjs` «accepted-rust-native: GAP-13 instruction-topics of the wrong type» (Node-строка — ровно одна, форма `instruction-topics: … is not a function`; Rust-строка — точный текст; иных расхождений нет) |

## Завендоренные skills

| Skill | Состояние | Пин |
|---|---|---|
| `versioning-standard-docs` | `vendored` | SHA артефакта + SHA исходного архива и записи в нём |
| `bugfix-protocol` | `vendored-derived` | SHA производного артефакта + отдельно SHA исходного, со списком трансформаций |

`vendored-derived` означает, что в Kernel лежит не дословная копия: из артефакта
удалены продуктовые данные. Оба дайджеста записаны раздельно, и ни один не
выдаётся за другой. Смена пина — изменение Kernel и требует записи в CHANGELOG.

## Чего этот контракт намеренно не покрывает

- Продуктовые релизы и их CI — вне области Kernel.
- Область действия завендоренного стандарта версионирования документов
  (`skills/versioning-standard-docs/`) ограничена документами в каноническом
  wiki. Версионирование Git-артефактов Kernel — отдельный предмет; он описан
  двумя стандартами:
  [`standards/workspace/version-control-flow.md`](standards/workspace/version-control-flow.md)
  (поток веток: постоянные линии `main` / `dev` — до завершения миграции
  физически `master` / `develop`; обычные ветки `feature` / `bugfix` / `chore` /
  `docs` / `refactor` / `test` / `ci` / `build` и специальные `release` /
  `hotfix` / `promotion`) и
  [`standards/workspace/release-versioning.md`](standards/workspace/release-versioning.md)
  (SemVer-линия Kernel, `VERSION`, Keep a Changelog, тег `vX.Y.Z`). Стандарты
  задают режим продвижения **репозитория**: `semver-release` (Kernel —
  repository-level `VERSION`, тег `vX.Y.Z`) и `revision-promotion` (репозиторий
  без решения о SemVer, включая Instance — верхнеуровневое состояние
  идентифицируется Git SHA advancement commit, repository-level `VERSION`/тег не
  создаются). Вложенные независимо версионируемые единицы (`stack-profiles/`,
  smoke-пакет) режим репозитория не переопределяет — они ведут свои
  `VERSION`/`CHANGELOG`/теги сами. Правила выбора MAJOR/MINOR/PATCH обоих
  стандартов ссылаются на ломающие изменения в терминах этого контракта, не
  переопределяя его.
