# Friction log

Append-only. One entry per REPORT where a Kernel rule, document, protocol,
or gate check cost more than it should have. Format and rules:
`standards/workspace/feedback-and-metrics.md` in the Kernel.

Empty is the expected steady state on a quiet day — do not force an entry.

<!-- entries below, newest last -->
