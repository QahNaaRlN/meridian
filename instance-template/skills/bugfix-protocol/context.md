# Product context for bugfix-protocol

Instance data: the current product's conventions substituted into the Kernel
protocol when it is applied. Rewrite this file entirely when the product
changes; the protocol itself does not change.

Every line below is a REPLACE_ME blocker until filled from the product's own
sources — the protocol forbids reconstructing conventions from memory or by
analogy with another product.

- REPLACE_ME — which libraries are in use for the affected layers
- REPLACE_ME — the single error channel
- REPLACE_ME — mandatory test utilities and how stores/hooks are wrapped
- REPLACE_ME — where a regression test physically lives
