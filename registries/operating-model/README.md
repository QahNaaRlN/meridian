---
title: Реестры операционной модели
document_type: readme
status: maintained
scope: workspace
owner: workspace-owner
created: 2026-09-08
updated: 2026-09-12
---

# Реестры операционной модели

Каталог содержит схемы машинных контрактов универсальной операционной модели
Meridian. Данные конкретного продукта здесь не хранятся.

`foundation.schema.json` проверяет машинную половину канонического глоссария
и реестра принципов —
`standards/workspace/operating-foundation.yaml`. Определения терминов находятся
в `standards/workspace/operating-glossary.md`, нормативные последствия
принципов — в `standards/workspace/operating-principles.md`. Валидатор Kernel
проверяет не только форму YAML, но и совпадение идентификаторов и двуязычных
названий с двумя человекочитаемыми документами.

Будущие схемы типов задач, состояния исполнения и управления человеком
добавляются отдельными пакетами программы `meridian-operating-upgrade`; эта
схема не предопределяет их поля.

`workspace-scope-model.schema.json` проверяет канонический пул логических
областей из `standards/workspace/workspace-scope-model.yaml`.
`scoped-record.schema.json` задаёт нейтральный к способу хранения конверт
записи с отдельными областью, происхождением и полномочием. Их нормативный
смысл и переход от отдельного Экземпляра описаны в
`standards/workspace/workspace-scope-model.md`.

`instruction-source-registry.schema.json` — специализированная **обязательная**
схема снимка **источника инструкций** (`instruction-source`): объявленный
носитель и **взаимоисключающие формы** `location` (`medium: file` требует
`path` + непрозрачный `container_ref` и запрещает `service_ref`/`resource_ref`;
`external-service` — наоборот); явная редакция с SHA-256-дайджестом и поле
`currency` (`current`/`stale`/`unverified`, где `stale` — только при
`source-missing`/`source-unreadable`); раздельно смоделированные формат и
`read_channel`, который описывает **только наблюдение самого источника** (`agent-native`
— агент читает файл сам; `meridian-observed` — Meridian сам читает источник для
снимка; `manual` — сообщает человек) и **не** является каналом доставки нормы —
ни регистрация, ни чтение не дают тексту полномочий и не обходят
`controlled-rule-intake`; отдельное отображение `agent-native` с явной границей
видимости Meridian (`meridian_visibility ≠ full`); состояние расхождения: два
проверенных состояния совпадают (`unchanged`) только при совпадении **и**
`revision`, **и** SHA-256-дайджеста, `unknown` недопустим при двух полных
проверенных состояниях, а `source-missing`/`source-unreadable` не несут
текущего состояния. Временна́я связь одного наблюдения проверяется кодом:
`recorded_state` ↔ `divergence.current_state` согласуются по `revision`,
дайджесту **и признаку `verified`** в обе стороны, а при
`source-missing`/`source-unreadable` — `previous_state` совпадает с сохранённым
`recorded_state`. Общий конверт каждой записи по-прежнему проверяется
`scoped-record.schema.json`, а не переопределяется здесь. Правила, которые
подмножество JSON Schema выразить не может, и продуктово-нейтральные фикстуры из
`fixtures/instruction-source-registry.fixtures.json` живут в одной функции
`scripts/lib/instruction-source-registry.mjs`, которую вызывают и участок
`instruction-source-registry` в `scripts/kernel-validate.mjs`, и набор
`test/instruction-source-registry.test.mjs`. Схема — Ядро, **данные реестра —
Экземпляр** (как для реестра приёмки). Контракт обязателен: **отсутствие схемы
или фикстур в Ядре — ошибка проверки Ядра**. Регистрация источника не делает
его текст принятой нормой. Нормативный смысл — в
`standards/workspace/instruction-source-registry.md`.

`task-pattern-registry.schema.json` проверяет **обязательный** каталог
универсальных шаблонов типов задач из
`standards/workspace/task-pattern-registry.yaml`: контейнер и тело шаблона в
`payload` каждой записи (виды работы, класс изменения только при
`work_kind: change`, инварианты, обязательные входы, требуемые доказательства,
условия остановки и три раздельные оси ссылок — `applicable_protocols`,
`applicable_skills`, `applicable_evidence_contracts`). Это специализированная
схема **содержимого**: общий конверт каждой записи по-прежнему проверяется
`scoped-record.schema.json`, а не переопределяется здесь. Межзаписные правила
(ровно семь классификационных пар, разделение осей — способ выполнения не
кладётся в массив протоколов и наоборот, принадлежность каждой `present`-ссылки
отслеживаемому набору файлов Ядра после разрешения симлинков, маршруты
`REFACTOR` и `BUGFIX`, а также текстовая защита от возврата формулировки
`BUGFIX → bugfix-protocol` в `standards/workspace/rule-resolution.md` §7) и
продуктово-нейтральные фикстуры из
`fixtures/task-pattern-registry.fixtures.json` живут в одной функции
`scripts/lib/task-pattern-registry.mjs`, которую вызывают и участок
`task-pattern-registry` в `scripts/kernel-validate.mjs`, и набор
`test/task-pattern-registry.test.mjs`. Отсутствие каталога, схемы или фикстур —
ошибка проверки Ядра. Нормативный смысл каталога — в
`standards/workspace/task-pattern-registry.md`.

`task-specification.schema.json` — **полная обязательная** схема одной
**постановки задачи** (`task-specification`): переносимая человеко- и
машиночитаемая форма конкретной единицы работы. Запись объявляет **эту** схему
в своём `$schema` (переносимой относительной ссылкой), а не схему конверта:
специализированная схема **композирует** общий конверт `scoped-record`
(`id`/`title` — идентичность и русское название; `origin`/`authority`) с телом,
так что потребитель, идущий за `$schema`, за один проход проверяет и конверт, и
тело и не принимает запись без `goal`/`initial_state`/`target_model`. Конверт
при этом **переиспользуется и проверяется отдельно**: `scripts/lib/task-specification.mjs`
дополнительно прогоняет каждую запись против канонической
`scoped-record.schema.json`. Область постановки — поле `scope` конверта,
суженное до двух значений: `project-workspace` и `repository-scope`;
`built-in-methodology`, `user-profile`, `organization-profile` и `run-state`
отклоняются (профили хранят правила и настройки; `run-state` — эпизодическое
состояние запуска, а постановка существует до запуска и может иметь несколько
запусков). Тело в `payload`: `goal`, явное `initial_state`, явная
`target_model`, ссылка `task_pattern` ровно на один существующий шаблон из
каталога типов задач, непустой массив `constraints` и непустой массив
`acceptance_criteria`, где каждый критерий несёт устойчивый семантический `id`,
`statement` и структурированную `verification` (`method` + `expected_result`) —
свободная фраза без проверяемого условия критерием не является. Постановка
описывает **требуемое содержание** работы и **не** несёт состояния запуска
(этап, рабочее состояние, участник, история переходов, следующий шаг) — это
область пакета `execution-state-model`. Правила, которые подмножество JSON
Schema выразить не может (**разрешение** объявленного `$schema` внутри
пространства имён Meridian от канонической **логической** базы
(`CANONICAL_RECORD_BASE`, логический адрес — не каталог файловой системы, не
репозиторий и не способ хранения) до логического адреса
`task-specification.schema.json` Ядра, а не сверка по базовому имени;
разрешение ссылки на шаблон по действующему каталогу с ошибкой при неизвестном
или неоднозначном идентификаторе; согласование `work_kind`/`change_class`;
допустимые области; переносимость строк тела **и ссылочных строк конверта**
`origin.source_ref`/`authority.authority_ref`/`authority.decision_ref` —
отклоняется любой корневой POSIX-путь независимо от разделителя перед ним и
**без перечня допустимых символов первого сегмента** (`/@scope`, `/$private`,
`/~service`, `/💾`, `/Проект`, `/数据` — корневые; `packages/@scope/module` и
`relative/$private/file` — относительные, не отклоняются), `~/`, путь Windows,
`\` и `file://`; непустота и отсутствие повторов критериев; отклонение данных
запуска; русское название), и
продуктово-нейтральные фикстуры из `fixtures/task-specification.fixtures.json`
живут в одной функции `scripts/lib/task-specification.mjs`, которую вызывают и
участок `task-specification-contract` в `scripts/kernel-validate.mjs`, и набор
`test/task-specification.test.mjs`. Схема — Ядро, **конкретные постановки —
Экземпляр**. Отсутствие схемы, каталога типов задач или фикстур — ошибка
проверки Ядра. Нормативный смысл — в
`standards/workspace/task-specification.md`.

`execution-state.schema.json` — **полная обязательная** схема состояния одного
**запуска** (`execution-run`): переносимый, независимый от способа хранения
снимок одной фактической попытки выполнить постановку. Управляемая сущность —
запуск, а не разговорная «задача»: одна постановка может иметь несколько
запусков, каждый со своим `id`, русским `title` и **ровно одной** переносимой
ссылкой `task_specification_ref` (постановка не встраивается и не дублируется).
Запись объявляет **эту** схему в своём `$schema` переносимой относительной
ссылкой; специализированная схема **композирует** общий конверт `scoped-record`
с телом, а `scripts/lib/execution-state.mjs` дополнительно прогоняет запись
против канонической `scoped-record.schema.json`. Область — только `run-state`
(`scope.id` идентифицирует запуск, `scope.workspace_id` обязателен); прочие
области отклоняются. `payload` закрыт и несёт **независимые оси**
(`lifecycle_stage` — закрытый упорядоченный пул `intake … completion`;
`work_status` — закрытый пул `planned, ready, active, waiting_human, blocked,
failed, completed, cancelled`; `scope_revision`; `current_actor` —
непрозрачная переносимая ссылка без роли и полномочия; `resolved_norms`,
`completed_checks` — уникальные переносимые ссылки; `blockers`; `next_action`,
`next_gate` — исполнимое значение или явный `null`; `transition_history`), так
что одно универсальное поле `status` и преждевременные поля роли, режима
надзора, способа связи, манифеста контекста, доказательств и передачи
отклоняются по построению. Правила, которые подмножество JSON Schema выразить не
может — **разрешение** объявленного `$schema` в логическом пространстве имён
(функции `resolveSchemaRef`/`nonPortableReason` **переиспользуются** из
`task-specification.mjs`, расходящейся копии нет); согласованность осей
(`blocked` ⇔ непустой `blockers`; `waiting_human` называет конкретное действие;
`completed`/`cancelled` не несут исполнимого шага; уникальность ссылок и
`blocker.id`; проверяемое `resumption_condition`); упорядоченность истории по
строго возрастающему `sequence`, явное исходное состояние, непрерывность цепочки
`from`/`to`, совпадение последней записи с текущими осями, неубывание
`scope_revision`, явное основание для перехода назад, пропуска этапа и перехода
после завершения; переносимость всех строк; русское название — и
продуктово-нейтральные фикстуры из
`fixtures/execution-state.fixtures.json` живут в одной функции
`scripts/lib/execution-state.mjs`, которую вызывают и участок
`execution-state-model` в `scripts/kernel-validate.mjs`, и набор
`test/execution-state.test.mjs`. Схема — Ядро, **записи запусков — Экземпляр**.
Отсутствие схемы или фикстур — ошибка проверки Ядра. Нормативный смысл — в
`standards/workspace/execution-state-model.md`.

`role-registry.schema.json` и `human-control.schema.json` — **полные
обязательные** схемы пакета `role-and-human-control`. `role-registry.schema.json`
проверяет **встроенный каталог** семи универсальных ролей (`owner`, `operator`,
`executor`, `reviewer`, `verifier`, `git_integrator`, `deployer`): одна запись
(`record_type: role-registry`), композирующая общий конверт `scoped-record`,
живёт **только** в `built-in-methodology`, каждая роль присутствует ровно один
раз и несёт русское название и список ответственности; конкретная программа,
модель ИИ, поставщик или имя человека в универсальное определение роли не входят.
Сам каталог — `standards/workspace/role-registry.yaml`.
`human-control.schema.json` проверяет **состояние управления одним запуском**
(`record_type: run-human-control`) в области `run-state` (`scope.workspace_id`
обязателен, `origin.kind: built-in` отклоняется) с переносимой ссылкой
`execution_run_ref` ровно на один запуск (тело `execution-run` не встраивается).
`payload` закрыт и несёт независимые оси: `human_authority` — постоянная
константа `posture: human-in-command` (не режим и не отключается) и `holder` —
участник, сохраняющий HIC, обязанный быть назначенным с ролью `owner`;
`supervision_mode` — закрытый переключаемый пул `human-in-the-loop` (требует блок
`hitl` с конкретным действием человека и рубежом и **запрещает** `hotl`) |
`human-on-the-loop` (требует блок `hotl` с границами автономии и возможностью
вмешательства и **запрещает** `hitl`); `acting_actor` — действующий участник,
обязанный быть в `role_assignments`; `role_assignments` — непустой список
`{ actor, roles }` из закрытого пула, где один участник может совмещать роли, а
пустое или неоднозначное назначение, повторный участник и повторная пара
(участник, роль) отклоняются; необязательный `review_independence` (базовая
модель «владелец и один исполнитель» принимается без проверяющего; при
`required: true` нужен отдельный подходящий участник, не совпадающий с
проверяемым; при `required: false` блок не несёт ссылок; любая присутствующая
ссылка проверяется на переносимость независимо от `required`);
`communication_mode` — закрытый пул `owner_relayed` | `direct`; `switch_history`
— упорядоченная строго возрастающим `sequence` история, где каждая запись несёт
пару `from_`/`to_` для **каждой** изменяемой оси (режим надзора, способ связи,
действующий участник, полный набор назначений ролей), первая запись задаёт
исходное назначение (все четыре `from_*` равны `null`), каждая следующая
продолжает предыдущую **без разрыва на каждой оси**, каждая несёт основание и
сохранённую контрольную точку, `to_acting_actor` каждой записи присутствует в её
`to_role_assignments`, а последняя запись совпадает со **всем** текущим
состоянием управления — режимом надзора, способом связи, действующим участником
**и** набором назначений ролей. Правила, которые подмножество JSON Schema
выразить не может (**разрешение** объявленного `$schema` в пространстве имён
Meridian — `resolveSchemaRef`/`nonPortableReason` **переиспользуются** из
`task-specification.mjs`; полное покрытие и непротиворечивость пула ролей;
привязка носителя HIC к назначенному владельцу; взаимоисключаемость `hitl`/`hotl`
и согласованность осей HIC/HITL/HOTL; независимость проверки и её отсутствие;
принадлежность действующего и переходного участника назначениям; непрерывность
истории по каждой оси и совпадение последней записи со всем состоянием;
переносимость всех строк; русские названия), и продуктово-нейтральные фикстуры из
`fixtures/role-and-human-control.fixtures.json` живут в одной функции
`scripts/lib/role-and-human-control.mjs`, которую вызывают и участок
`role-and-human-control` в `scripts/kernel-validate.mjs`, и набор
`test/role-and-human-control.test.mjs`. Схемы и каталог — Ядро, **конкретные
записи управления — Экземпляр**. Отсутствие схемы, каталога или фикстур — ошибка
проверки Ядра. Нормативный смысл — в
`standards/workspace/role-and-human-control.md`.

`context-manifest.schema.json` — **полная обязательная** схема **ограниченного
манифеста контекста одного запуска** (`context-manifest`): переносимый,
независимый от способа хранения перечень авторитетных входов и состояния,
которого достаточно, чтобы продолжить именно этот запуск после паузы, смены
сеанса, участника или роли **без восстановления авторитетного состояния из
истории чата**. Запись объявляет **эту** схему в своём `$schema` переносимой
относительной ссылкой; специализированная схема **композирует** общий конверт
`scoped-record` с телом, а `scripts/lib/context-manifest.mjs` дополнительно
прогоняет запись против канонической `scoped-record.schema.json`. Область —
только `run-state` (`scope.workspace_id` обязателен, `origin.kind: built-in`
отклоняется). **Связь с одним запуском детерминирована**, а не выведена по
подстроке: `execution_run_ref`, `task_specification_ref` и `human_control_ref` —
**закрытые структурированные закреплённые ссылки** `pinned_ref`
(`{ record_type, id, reference, run_id?, revision?/sha256? }`); `scope.id`
**точно равен** `execution_run_ref.id`; `human_control_ref.run_id` равен
`execution_run_ref.id`; `task_specification_ref` `run_id` не несёт; строка
вместо объекта и лишнее поле (тело записи) отклоняются. **Точная ревизия —
закрытый набор**: полный Git SHA (40 или 64 hex, не сокращение), строгий тег
`v?X.Y.Z` или `sha256`-дайджест; ветка или канал (строка со слэшем, одно слово,
подвижный токен `latest`/`HEAD`/`main`/… ) плавающие независимо от цифр; иной
провайдер-зависимый или сокращённый вид `revision` требует `sha256`. Одно
правило применяется к `pinned_ref`, `applicable_norms` и изменяемым
`authoritative_sources` — расходящейся реализации нет. `payload` закрыт и несёт:
три `pinned_ref`; `scope_revision`, явные `next_action` и `next_gate`;
**непустой** `authoritative_sources` (каждый изменяемый источник закреплён по
тому же правилу; неизменяемый закрепления не требует, но веточная ревизия в нём
отклоняется); `applicable_norms` (ссылка, происхождение, закрепление, объяснение
применимости), чьё множество `reference` **точно равно**
`run_state_checkpoint.resolved_norms` — манифест добавляет объяснение, но не
подменяет разрешённый набор; **раздельные** списки `decisions`,
`open_questions`, `completed_actions`, `completed_checks`, `known_gaps` и
`blockers` без повторов; и `run_state_checkpoint` — **закреплённый авторитетный
снимок**, несущий **собственные** `pinned_ref` на три записи, `scope_revision`,
`lifecycle_stage`, `work_status`, `next_action`, `next_gate`, `blocker_ids`,
`resolved_norms` и `completed_checks`. Снимок **не доказывается** второй копией
тех же полей внутри манифеста: **отдельно** разрешается каждая из шести ссылок —
трёх манифеста и трёх снимка — через границу, внешнюю по отношению к записи
(резолвер из ключа `resolution` файла фикстур, `makeRecordResolver`). Ответ
преобразователя — **закрытый контракт**: обязательны `record_type`, непустой
`id`, подтверждение закрепления, совпадение заявленного `reference`,
`resolved_state` со всеми осями для `execution-run` и `linked_run_ref` для
`run-human-control`; отсутствие поля — точная ошибка. Оси снимка сверяются с
записью `execution-run`, разрешённой **из
`run_state_checkpoint.execution_run_ref`**, с подтверждением той же редакции, что
у `execution_run_ref` верхнего уровня; ссылка снимка на другой существующий
запуск, постановку или запись управления отклоняется. Вымышленный `sha256`,
согласованное изменение обеих копий и неполный ответ преобразователя
отклоняются. С манифестом снимок обязан совпадать по редакции области, следующему
шагу, рубежу, множеству препятствий и множеству завершённых проверок; сам снимок
подчиняется осевым правилам модели состояния выполнения. Отсутствие закреплённого
`execution-run` или `run-human-control`, а также отсутствие внешнего резолвера,
делает манифест непостроимым. Необъявленный избыток контекста
(полные тексты файлов, журнал команд, `transcript`/`messages`/`chat_history`,
выгрузка каталога) и поля полного контракта доказательств и передачи
(`evidence`, `handoff`, состояние рабочего дерева, итоговый вердикт) или
показателей полевой оценки отклоняются по построению — это предмет следующих
пакетов. Правила, которые подмножество JSON Schema выразить не может
(**разрешение** объявленного `$schema` в пространстве имён Meridian —
`resolveSchemaRef`/`nonPortableReason` **переиспользуются** из
`task-specification.mjs`; закрытые пулы этапов и рабочих состояний
**переимпортированы** из `execution-state.mjs`; детерминированная связь с одним
запуском; закрытое правило распознавания точной ревизии; закрепление изменяемых
источников и норм; отсутствие повторов источников, норм, решений, вопросов,
действий, проверок, пробелов и препятствий; **внешнее разрешение** закреплённых
записей `execution-run`/`task-specification`/`run-human-control` и сверка снимка
с фактической записью запуска; согласованность манифеста с закреплённым снимком,
включая ссылку на постановку и множество норм; осевые правила самого снимка;
переносимость всех строк; русское название), и
продуктово-нейтральные фикстуры из `fixtures/context-manifest.fixtures.json`
живут в одной функции `scripts/lib/context-manifest.mjs`, которую вызывают и
участок `bounded-context-manifest` в `scripts/kernel-validate.mjs`, и набор
`test/context-manifest.test.mjs`. Схема — Ядро, **конкретные манифесты
запусков — Экземпляр**. Отсутствие схемы или фикстур — ошибка проверки Ядра.
Нормативный смысл — в `standards/workspace/bounded-context-manifest.md`.

`evidence-and-handoff.schema.json` — **полная обязательная** схема **передачи
состояния и результата одного запуска** (`evidence-and-handoff`): переносимая,
независимая от способа хранения запись того, **что произошло, что доказано и что
делать дальше**. Запись объявляет **эту** схему в своём `$schema` переносимой
относительной ссылкой; специализированная схема **композирует** общий конверт
`scoped-record` с телом, а `scripts/lib/evidence-and-handoff.mjs` дополнительно
прогоняет запись против канонической `scoped-record.schema.json`. Область —
только `run-state` (`scope.id` **точно равен** `execution_run_ref.id`,
`scope.workspace_id` обязателен, `origin.kind: built-in` отклоняется). `payload`
несёт **четыре** закрытые структурированные закреплённые ссылки `pinned_ref` —
на запуск, постановку задачи, запись управления человеком **и манифест
контекста** (тела не встраиваются); `human_control_ref` и `context_manifest_ref`
несут `run_id`, равный `execution_run_ref.id`, `task_specification_ref` — нет.
**Закрытое правило точной ревизии переиспользуется** из
`scripts/lib/context-manifest.mjs` (`classifyRevision`) и применяется к каждой
`pinned_ref` и к ревизиям репозиториев в `source_state` / `result_state`;
расходящейся реализации нет. Заявленные результаты (`claimed_results`),
проверяемые утверждения (`verifiable_assertions`) и доказательства (`evidence`)
— **три раздельных списка**, связанных **явно по устойчивым идентификаторам**:
каждое утверждение называет свой `claimed_result_id`, каждый элемент
доказательства называет покрываемые утверждения в `covers`. **Каждый** элемент
`evidence` сам закреплён точной ревизией или `sha256` и **разрешается через
внешнюю границу** в закрытый ответ `evidence-result` с `observed_result`
(`confirmed` / `contradicted` / `inconclusive`) **и предметом подтверждения** —
`covers` (утверждения, к которым относится результат) и (или) `check_ref`
(обязательная проверка, чьим результатом оно является). Утверждение получает
`verified` **только** когда покрывающий его элемент разрешился без замечаний, его
`observed_result` равен `confirmed` **и** преобразователь подтвердил связь
`covers` — перенос `covers` разрешённого доказательства на постороннее
утверждение отклоняется; обычная строка `reference` без закрепления и разрешения
подтверждением не является. Статус заявленного результата **выводится с обеих
сторон**: `established` — при непустом наборе привязанных утверждений, все
`verified`; `not_established` при полностью проверенном наборе **отклоняется**.
`payload.acceptance_criteria` **замыкает** список критериев приёмки на набор
идентификаторов, который подтверждает закрытый ответ преобразователя для
`task-specification` (**без тела постановки**): каждый критерий `covered`
проверяемым утверждением либо `uncovered` с причиной; неизвестный, повторный или
пропущенный идентификатор отклоняется. Каноническая постановка требует
**непустой** набор критериев приёмки — закрытый ответ преобразователя с пустым
`acceptance_criteria` отклоняется, поэтому завершённая передача с пустыми
`acceptance_criteria` и `mandatory_checks` не проходит. **Полный набор**
`mandatory_checks` тоже **замкнут** на авторитетный источник — минимальное
машинное представление ожидаемого набора в том же закрытом ответе преобразователя
(**без тела постановки**): удаление любой обязательной проверки из передачи
отклоняется, «список непуст» недостаточно. `mandatory_checks` различают **ровно
три** состояния — `passed` / `failed` / `unable`: `passed` и `failed` несут
`result_evidence_id`, чей разрешённый `evidence-result` подтверждает результат
(`observed_result` `confirmed` / `contradicted`) **и несёт `check_ref` именно
этой проверки** (результат одной проверки не является результатом другой);
`unable` требует проверяемой причины, `result_evidence_id` не несёт и никогда не
становится успехом. `completed_checks` разрешённого запуска подтверждает **факт
запуска** проверки, а не вердикт: `check_ref` проверки `passed` **или** `failed`
там присутствует, отсутствует там только `unable`. `source_state` и
`result_state` закреплены **точными ревизиями**
по репозиториям и закрепляют **один и тот же набор** `repository_ref`; каждый
репозиторий из `changed_paths` присутствует в обоих с **различающимся**
закреплением. `changed_paths`, `external_effects`, `deviations`, `open_gaps`,
`required_owner_decisions` и `blockers` — **шесть раздельных обязательных
списков** без повторов идентификатора; опущенный ключ — ошибка, пустой список —
явное утверждение об отсутствии события. `worktree_disposition` использует
**принятый закрытый набор** `not_created` / `removed` / `retained`
(`version-control-flow.md` §5.4; `retained` требует причины, ответственного и
проверяемого условия очистки). `next_step` (`action` / `gate` / `actor_ref`)
представлен явно и сверяется с `next_action` / `next_gate` разрешённой записи
`execution-run`. `outcome.status: complete` означает **весь запуск завершён**:
разрешённая запись `execution-run` имеет `work_status: completed`, `next_step`
равен `null`, `open_gaps` пуст, каждый критерий приёмки `covered` проверенным
утверждением. Специализированные контракты доказательств (исправления ошибки,
дымовой проверки, функционального паритета) **не дублируются**: элемент
`kind: specialised-evidence-record` несёт их название и зафиксированный вердикт,
которые **подтверждает разрешённый `evidence-result` точным совпадением**;
общий контракт вердикт **не выводит заново и не переопределяет**; полное тело
специализированной записи в `payload` отклоняется. Четыре закреплённые ссылки
**и каждый элемент `evidence`** **разрешаются через границу, внешнюю по отношению
к записи** (резолвер из ключа `resolution` файла фикстур, `makeRecordResolver`) —
тем же замкнутым контрактом ответа преобразователя, что у манифеста контекста;
без резолвера запись непроверяема. Необъявленный избыток контекста (полные
тексты файлов, журнал команд, `transcript`/`messages`/`chat_history`), тела
ссылочных записей и поля полевой оценки отклоняются по построению. Правила, которые подмножество JSON
Schema выразить не может, и продуктово-нейтральные фикстуры из
`fixtures/evidence-and-handoff.fixtures.json` живут в одной функции
`scripts/lib/evidence-and-handoff.mjs`, которую вызывают и участок
`evidence-and-handoff-contract` в `scripts/kernel-validate.mjs`, и набор
`test/evidence-and-handoff.test.mjs`. Схема — Ядро, **конкретные записи
передачи — Экземпляр**. Отсутствие схемы или фикстур — ошибка проверки Ядра.
Нормативный смысл — в `standards/workspace/evidence-and-handoff-contract.md`.

`field-evaluation.schema.json` — **полная обязательная** схема **двух**
типов записи практической оценки Meridian: `field-evaluation-observation`
(одна измеренная точка данных об одном из восьми показателей
`meridian-operating-upgrade-plan.md` §10 для одного запуска, область
`run-state`, закреплённая ссылка `execution_run_ref`) и
`field-evaluation-report` (детерминированная агрегация по закреплённому,
внешне разрешённому набору наблюдений, область `project-workspace`, никогда
один итоговый балл). Обе записи объявляют **эту** схему в своём `$schema`
переносимой относительной ссылкой; какое тело применяется, решает
`record_type` (`allOf`/`if`/`then`), а не второй файл схемы; специализированная
схема композирует общий конверт `scoped-record` с телом, а
`scripts/lib/field-evaluation.mjs` дополнительно прогоняет каждую запись
против канонической `scoped-record.schema.json`. Каждый из восьми показателей
имеет закрытый вид измерения (классификация / длительность / счётчик) и, для
классификации, закрытый пул исходов, зафиксированные программно, а не
выведенные по тексту. Каждая календарная дата и метка времени контракта
проверяется на **реальное существование** (длина конкретного месяца,
високосный год), а не только на синтаксический шаблон — 31 февраля, 31 апреля
и невисокосное 29 февраля отклоняются, а не округляются молча. Наблюдение
`observed` требует закреплённое (точная ревизия или `sha256`) доказательство,
**разрешаемое через внешнюю границу** в закрытый ответ `evidence-result`,
несущий `observed_result` и `metric_ref` — предмет подтверждения:
доказательство другого показателя не засчитывается. Исправление ошибочной
записи — новое наблюдение с закреплённой ссылкой `supersedes` и
`correction_reason`, без тихой перезаписи истории. Отчёт закрывает ровно
восемь записей `per_metric` (по одной на показатель), проверяет
сопоставимость включённых наблюдений (то же рабочее пространство, та же
отчётная период), держит `included_observations` и `excluded_observations`
**непересекающимися** — ни по буквальной ссылке, ни по фактически
разрешённому `id` — отклоняет повторный учёт (повтор ссылки или
одновременное включение исходного и исправленного наблюдения одной цепочки),
требует явного раскрытия исключения наблюдения в `limitations`, **проверяет
разрешённую проекцию** каждого наблюдения, на которое ссылается отчёт
(`metric_id`, `status`, `measurement`, `workspace_id`, `observed_at`,
`supersedes_id`, окно и покрытие дефектов), до какого-либо использования в
агрегации, и **пересчитывает** — при **любом** статусе записи (`known`,
`unknown` или `not_applicable`) — каждый агрегат `per_metric` из фактически
разрешённой выборки, сверяя его **точным совпадением** с заявленным —
расходящийся с наблюдениями отчёт отклоняется независимо от заявленного
статуса. Доля всегда несёт числитель и знаменатель; нулевой знаменатель не
даёт процента. Правила, которые подмножество JSON Schema выразить не может, и
продуктово-нейтральные фикстуры из `fixtures/field-evaluation.fixtures.json`
живут в одной функции `scripts/lib/field-evaluation.mjs` (включая
детерминированный построитель отчёта `buildFieldEvaluationReport`), которую
вызывают и участок `meridian-field-evaluation` в `scripts/kernel-validate.mjs`,
и набор `test/field-evaluation.test.mjs`. Построитель сортирует оба входных
списка по каноническому ключу — разрешённый `id` с `reference` как
однозначным резервным сравнением — и разрешает каждую ссылку **тремя**
раздельными шагами одной границы, ни один из которых не подменяет другой:
`checkPinnedRef` на самой входной `pinned_ref` (её собственный `record_type`,
устойчивый `id`, переносимая `reference` и закрепление — независимо от того,
что вернёт резолвер), `resolvePinnedReference` на разрешённой записи (её
`record_type`, замкнутый набор ключей, совпадение `id`/`reference` и
закрепления) и `resolvedObservationIssues` на предметных значениях. Все три
шага обязательны: ответ резолвера с верными предметными полями и совпадающим
`id`, но с неверным `record_type` входной ссылки, без `id` или без пина, не
может быть принят построителем в обход границы. Построитель завершается
закрытым исключением — при недействительной, неразрешимой, повторной или
пересекающейся ссылке, при неверной входной или разрешённой записи, а также
при паре «наблюдение + наблюдение, которое его `supersedes`» в списке
включённых — вместо молчаливого пропуска, автоматического выбора исправления
или возврата отчёта, который лишь затем отклонит проверка. Файл фикстур несёт
отдельные именованные записи для каждого обхода и явно названную секцию
`builder_scenarios` для случаев, специфичных для построителя, а не для
самостоятельной записи схемы. Схема — Ядро, **конкретные наблюдения и
отчёты — Экземпляр**. Отсутствие схемы или фикстур — ошибка проверки Ядра.
Нормативный смысл — в `standards/workspace/meridian-field-evaluation.md`.
